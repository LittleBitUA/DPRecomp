diff --git a/include/rex/graphics/d3d12/pipeline_cache.h b/include/rex/graphics/d3d12/pipeline_cache.h
index 7a9d6a2..d5128a3 100644
--- a/include/rex/graphics/d3d12/pipeline_cache.h
+++ b/include/rex/graphics/d3d12/pipeline_cache.h
@@ -381,6 +381,20 @@ class PipelineCache {
   FILE* pipeline_storage_file_ = nullptr;
   bool pipeline_storage_file_flush_needed_ = false;
 
+  // DP1 v1.1 (Downpour port): ID3D12PipelineLibrary blob cache of driver-
+  // compiled PSOs, in cache/shaders/local/ (GPU/driver specific, never
+  // shipped). LoadGraphicsPipeline hit = microseconds instead of a 100-800 ms
+  // driver compile. Library calls are serialized by pipeline_library_mutex_;
+  // the CreateGraphicsPipelineState fallback stays outside the lock.
+  Microsoft::WRL::ComPtr<ID3D12PipelineLibrary> pipeline_library_;
+  std::vector<uint8_t> pipeline_library_blob_;
+  std::filesystem::path pipeline_library_path_;
+  std::mutex pipeline_library_mutex_;
+  std::atomic<uint32_t> pipeline_library_load_hits_{0};
+  std::atomic<uint32_t> pipeline_library_store_count_{0};
+  std::atomic<bool> pipeline_library_dirty_{false};
+  void FlushPipelineLibraryToDisk();
+
   // Thread for asynchronous writing to the storage streams.
   void StorageWriteThread();
   std::mutex storage_write_request_lock_;
diff --git a/include/rex/graphics/d3d12/render_target_cache.h b/include/rex/graphics/d3d12/render_target_cache.h
index cd7693a..9eec523 100644
--- a/include/rex/graphics/d3d12/render_target_cache.h
+++ b/include/rex/graphics/d3d12/render_target_cache.h
@@ -750,6 +750,15 @@ class D3D12RenderTargetCache final : public RenderTargetCache {
   ID3D12PipelineState* resolve_rov_clear_32bpp_pipeline_ = nullptr;
   // Clearing 64bpp color.
   ID3D12PipelineState* resolve_rov_clear_64bpp_pipeline_ = nullptr;
+
+  // DP1 fix: value-clipping 7e3 -> 8888 in-place EDRAM conversion (uses the
+  // resolve EDRAM buffer clear root signature: root constants + uint4 UAV).
+  ID3D12PipelineState* rov_7e3_to_8888_pipeline_ = nullptr;
+  void PixelShaderInterlockReinterpret7e3To8888(uint32_t start_tiles,
+                                                uint32_t length_tiles) override;
+  // Records what a resolve clear writes for the 7e3 tracking.
+  void PixelShaderInterlockTrackClear(const draw_util::ResolveInfo& resolve_info, bool clear_depth,
+                                      bool clear_color);
 };
 
 }  // namespace rex::graphics::d3d12
diff --git a/include/rex/graphics/d3d12/texture_cache.h b/include/rex/graphics/d3d12/texture_cache.h
index 4d845c7..7435042 100644
--- a/include/rex/graphics/d3d12/texture_cache.h
+++ b/include/rex/graphics/d3d12/texture_cache.h
@@ -22,6 +22,7 @@
 #include <rex/graphics/d3d12/shader.h>
 #include <rex/graphics/d3d12/shared_memory.h>
 #include <rex/graphics/pipeline/texture/cache.h>
+#include <rex/graphics/pipeline/texture/replacement.h>
 #include <rex/graphics/pipeline/texture/util.h>
 #include <rex/graphics/register_file.h>
 #include <rex/graphics/xenos.h>
@@ -268,6 +269,18 @@ class D3D12TextureCache final : public TextureCache {
       srv_descriptors_.emplace(descriptor_key, descriptor_index);
     }
 
+    // Texture replacement (see pipeline/texture/replacement.h): when set,
+    // the resource is R8G8B8A8_UNORM with the image dimensions and
+    // replacement_mip_count() mips, filled from the image instead of guest
+    // memory.
+    const texture_replacement::Image* replacement() const { return replacement_; }
+    uint32_t replacement_mip_count() const { return replacement_mip_count_; }
+    void SetReplacement(const texture_replacement::Image* image, uint32_t mip_count) {
+      replacement_ = image;
+      replacement_mip_count_ = mip_count;
+    }
+    Microsoft::WRL::ComPtr<ID3D12Resource>& replacement_upload() { return replacement_upload_; }
+
    private:
     Microsoft::WRL::ComPtr<ID3D12Resource> resource_;
     D3D12_RESOURCE_STATES resource_state_;
@@ -278,8 +291,14 @@ class D3D12TextureCache final : public TextureCache {
     // according to profiling, was often a bottleneck in many games).
     // For bindless - indices in the global shader-visible descriptor heap.
     std::unordered_map<SRVDescriptorKey, uint32_t, SRVDescriptorKey::Hasher> srv_descriptors_;
+
+    const texture_replacement::Image* replacement_ = nullptr;
+    uint32_t replacement_mip_count_ = 0;
+    Microsoft::WRL::ComPtr<ID3D12Resource> replacement_upload_;
   };
 
+  bool UploadReplacementTexture(D3D12Texture& texture);
+
   static constexpr uint32_t kSRVDescriptorCachePageSize = 65536;
 
   struct SRVDescriptorCachePage {
diff --git a/include/rex/graphics/flags.h b/include/rex/graphics/flags.h
index 2cf24c4..e52c177 100644
--- a/include/rex/graphics/flags.h
+++ b/include/rex/graphics/flags.h
@@ -18,6 +18,10 @@ REXCVAR_DECLARE(bool, vsync);
 REXCVAR_DECLARE(bool, clear_memory_page_state);
 REXCVAR_DECLARE(bool, half_pixel_offset);
 REXCVAR_DECLARE(bool, async_shader_compilation);
+// DP1 (2026-09-06, ported from the Downpour fork): what to do with a draw whose
+// PSO is still compiling on a background thread. Defined in d3d12/pipeline_cache.cpp.
+REXCVAR_DECLARE(std::string, pso_missing_policy);
+REXCVAR_DECLARE(double, d3d12_pso_block_per_draw_budget_ms);
 
 // GPU Resolution / Readback / Queries
 REXCVAR_DECLARE(int32_t, resolution_scale);
@@ -46,6 +50,10 @@ REXCVAR_DECLARE(bool, direct_host_resolve);
 
 // GPU Textures
 REXCVAR_DECLARE(bool, gpu_allow_invalid_fetch_constants);
+REXCVAR_DECLARE(bool, texture_dump);
+REXCVAR_DECLARE(bool, texture_replacement);
+REXCVAR_DECLARE(std::string, texture_path);
+REXCVAR_DECLARE(std::string, texture_trace_hash);
 REXCVAR_DECLARE(bool, gpu_3d_to_2d_texture);
 REXCVAR_DECLARE(int32_t, anisotropic_override);
 REXCVAR_DECLARE(int32_t, texture_cache_memory_limit_render_to_texture);
diff --git a/include/rex/graphics/pipeline/render_target/cache.h b/include/rex/graphics/pipeline/render_target/cache.h
index 3bbd52e..0017c1b 100644
--- a/include/rex/graphics/pipeline/render_target/cache.h
+++ b/include/rex/graphics/pipeline/render_target/cache.h
@@ -548,6 +548,25 @@ class RenderTargetCache {
   // EDRAM memory are committed with a memory barrier.
   void PixelShaderInterlockFullEdramBarrierPlaced();
 
+  // DP1 fix (pixel shader interlock): value-clipping 7e3 -> 8888 EDRAM
+  // reinterpretation, the ROV counterpart of the host render target ownership
+  // transfer fix. The cache remembers which tiles were last written as
+  // k_2_10_10_10_FLOAT(_AS_16_16_16_16) color; when such tiles get bound as
+  // k_8_8_8_8(_GAMMA) color for drawing, the implementation is asked to
+  // convert their contents in place (unpack 7e3, saturate, pack UNORM8) before
+  // the draw, so pixels the draw leaves untouched (alpha test / src.a == 0
+  // blending) hold clipped LDR color instead of 7e3 bit patterns read as UNORM8.
+  // start_tiles + length_tiles never wrap past xenos::kEdramTileCount.
+  virtual void PixelShaderInterlockReinterpret7e3To8888(uint32_t start_tiles,
+                                                        uint32_t length_tiles) {}
+  // Records the format class of the color (or depth, for which is_color is
+  // false) data written to the tile range, and, if convert_if_7e3_to_8888 is
+  // true (drawing, as opposed to clearing), requests the reinterpretation of
+  // the sub-ranges that still hold 7e3 data when binding them as 8888.
+  void PixelShaderInterlockTrackTiles(uint32_t start_tiles, uint32_t length_tiles, bool is_color,
+                                      xenos::ColorRenderTargetFormat color_format,
+                                      bool convert_if_7e3_to_8888);
+
  private:
   const RegisterFile& register_file_;
   uint32_t draw_resolution_scale_x_;
@@ -661,6 +680,12 @@ class RenderTargetCache {
   // changes to this throughout a frame are pretty rare.
   std::map<uint32_t, OwnershipRange> ownership_ranges_;
 
+  // DP1 fix (pixel shader interlock): per-tile "last written as 7e3 color"
+  // flags, see PixelShaderInterlockTrackTiles. Independent of
+  // ownership_ranges_, which the interlock path resets on every full barrier
+  // and which doesn't carry the real color format there.
+  uint8_t interlock_tile_is_7e3_[xenos::kEdramTileCount] = {};
+
   // Render targets actually used by the draw call with the last successful
   // update. 0 is depth, color starting from 1, nullptr if not bound.
   // Only valid for non-pixel-shader-interlock paths.
diff --git a/include/rex/graphics/pipeline/texture/cache.h b/include/rex/graphics/pipeline/texture/cache.h
index d26cd4f..e457db7 100644
--- a/include/rex/graphics/pipeline/texture/cache.h
+++ b/include/rex/graphics/pipeline/texture/cache.h
@@ -19,6 +19,7 @@
 #include <vector>
 
 #include <rex/assert.h>
+#include <rex/graphics/pipeline/texture/replacement.h>
 #include <rex/graphics/pipeline/texture/util.h>
 #include <rex/graphics/register_file.h>
 #include <rex/graphics/shared_memory.h>
@@ -214,6 +215,11 @@ class TextureCache {
 
     uint64_t GetHostMemoryUsage() const { return host_memory_usage_; }
 
+    // Hash of the guest base level at creation time (0 when the texture
+    // dump/replacement registry is inactive). See texture/replacement.h.
+    uint64_t data_hash() const { return data_hash_; }
+    void SetDataHash(uint64_t hash) { data_hash_ = hash; }
+
     uint64_t last_usage_submission_index() const { return last_usage_submission_index_; }
     uint64_t last_usage_time() const { return last_usage_time_; }
     static constexpr uint32_t kOutdatedBitBase = UINT32_C(1) << 0;
@@ -255,6 +261,7 @@ class TextureCache {
     texture_util::TextureGuestLayout guest_layout_;
 
     uint64_t host_memory_usage_ = 0;
+    uint64_t data_hash_ = 0;
 
     uint64_t last_usage_submission_index_;
     uint64_t last_usage_time_;
@@ -582,6 +589,16 @@ class TextureCache {
 
   const RegisterFile& register_file_;
   SharedMemory& shared_memory_;
+
+ protected:
+  // Set by FindOrCreateTexture right before CreateTexture so the backend
+  // can pick a replacement image for the texture being created.
+  uint64_t creating_data_hash_ = 0;
+  texture_replacement::GuestBaseView creating_view_;
+  // Computes the base-level hash (and dumps the texture if enabled).
+  uint64_t HashGuestTexture(const TextureKey& key, texture_replacement::GuestBaseView& view_out) const;
+
+ private:
   uint32_t draw_resolution_scale_x_;
   uint32_t draw_resolution_scale_y_;
 
diff --git a/include/rex/graphics/shared_memory.h b/include/rex/graphics/shared_memory.h
index 4d85a2b..df951d7 100644
--- a/include/rex/graphics/shared_memory.h
+++ b/include/rex/graphics/shared_memory.h
@@ -95,6 +95,9 @@ class SharedMemory {
   // regions in those pages.
   void RangeWrittenByGpu(uint32_t start, uint32_t length);
 
+  // Guest memory (for CPU-side reads of texture data, e.g. hashing).
+  memory::Memory& guest_memory() const { return memory_; }
+
  protected:
   SharedMemory(memory::Memory& memory);
   // Call in implementation-specific initialization.
diff --git a/include/rex/input/mnk/mnk_input_driver.h b/include/rex/input/mnk/mnk_input_driver.h
index c8751b7..d5a4e75 100644
--- a/include/rex/input/mnk/mnk_input_driver.h
+++ b/include/rex/input/mnk/mnk_input_driver.h
@@ -24,6 +24,25 @@ namespace rex::input::mnk {
 
 void SetMouseLookActive(bool active);
 bool IsMouseLookActive();
+// DP1 2026-09-06: direct camera control. While active, mouse motion is NOT
+// mapped to the right stick; instead the app drains the raw deltas (pixels
+// since the last call) from a game-side hook and applies them to the camera
+// angles itself.
+void SetMouseCameraHookActive(bool active);
+bool IsMouseCameraHookActive();
+void TakeMouseCameraDelta(float& dx_out, float& dy_out);
+// DP1 v1.1.1: true while the camera hook has drained the delta within the
+// last ~200 ms, i.e. a gameplay camera is running. In menus / the map the
+// camera routines do not run, so the mouse falls back to the right stick
+// (map zoom and the like) instead of banking a camera snap.
+bool IsMouseCameraHookRecent();
+// DP1 v1.1 (DPRecomp #11): the aim camera of the game (PAL sub_8233B3C0) is
+// driven by the LEFT stick and keeps its own velocity model, so the camera
+// hook cannot steer it. A game hook calls NoteMouseAimFrame() every frame the
+// aim camera runs; while that is fresh, GetState maps the mouse onto the left
+// stick (the game's native aim input) instead of feeding the camera hook.
+void NoteMouseAimFrame();
+bool IsMouseAimStickActive();
 
 class MnkInputDriver final : public InputDriver,
                              public rex::ui::WindowInputListener,
@@ -50,6 +69,8 @@ class MnkInputDriver final : public InputDriver,
   void OnMouseDown(rex::ui::MouseEvent& e) override;
   void OnMouseUp(rex::ui::MouseEvent& e) override;
   void OnMouseMove(rex::ui::MouseEvent& e) override;
+  // DP1 port 2026-09-05 (from dpour fork ce7d25d, mnk_input_driver.h)
+  void OnMouseWheel(rex::ui::MouseEvent& e) override;
 
   // WindowListener
   void OnClosing(rex::ui::UIEvent& e) override;
@@ -75,6 +96,9 @@ class MnkInputDriver final : public InputDriver,
 
   std::mutex state_mutex_;
   bool key_down_[256] = {};
+  // Keys that went down since the last GetState poll (a tap shorter than a
+  // poll interval is otherwise invisible). Cleared by GetState.
+  bool key_pressed_since_poll_[256] = {};
 
   // Mouse delta tracking. Fractional because relative motion arrives in
   // fractions of a pixel, and truncating each event drops slow movement.
@@ -82,6 +106,14 @@ class MnkInputDriver final : public InputDriver,
   float mouse_dx_ = 0.0f;
   float mouse_dy_ = 0.0f;
 
+  // DP1 port 2026-09-05 (from dpour fork ce7d25d, mnk_input_driver.h)
+  // Vertical mouse-wheel accumulator (MouseEvent::kScrollPerDetent units,
+  // 120 per detent). Filled by OnMouseWheel on the UI thread, drained one
+  // detent per GetDeviceState poll on the guest thread (under state_mutex_),
+  // pulsing kWheelUp / kWheelDown for exactly one poll so the wheel can be
+  // bound to any controller button ("WheelUp" / "WheelDown").
+  int32_t wheel_accumulator_y_ = 0;
+
   // UI thread only.
   int32_t prev_mouse_x_ = 0;
   int32_t prev_mouse_y_ = 0;
diff --git a/include/rex/rex_app.h b/include/rex/rex_app.h
index 1f52480..48d7478 100644
--- a/include/rex/rex_app.h
+++ b/include/rex/rex_app.h
@@ -26,6 +26,7 @@
 #include <rex/ui/imgui_drawer.h>
 #include <rex/ui/immediate_drawer.h>
 #include <rex/ui/overlay/debug_overlay.h>
+#include <rex/ui/overlay/pso_compile_indicator_overlay.h>
 #include <rex/ui/window.h>
 #include <rex/ui/window_listener.h>
 #include <rex/ui/windowed_app.h>
@@ -297,6 +298,9 @@ class ReXApp : public ui::WindowedApp, public ui::WindowListener, public ui::Win
   std::unique_ptr<Runtime> runtime_;
   std::unique_ptr<ui::Window> window_;
   std::thread module_thread_;
+  // DP1 v1.1: second half of LaunchModule, run once the shader storage replay
+  // (shown by ShaderCompileDialog) has finished.
+  void FinishModuleLaunch(system::object_ref<system::XThread> main_thread);
   std::atomic<bool> shutting_down_{false};
   std::unique_ptr<ui::ImmediateDrawer> immediate_drawer_;
   std::unique_ptr<ui::ImGuiDrawer> imgui_drawer_;
@@ -304,6 +308,8 @@ class ReXApp : public ui::WindowedApp, public ui::WindowListener, public ui::Win
   // Built-in overlays
   std::shared_ptr<LogCaptureSink> log_sink_;
   std::unique_ptr<ui::DebugOverlayDialog> debug_overlay_;
+  // DP1 v1.1: corner badge while pipelines compile (Downpour port).
+  std::unique_ptr<ui::PsoCompileIndicatorDialog> pso_compile_indicator_overlay_;
   std::unique_ptr<ui::ConsoleDialog> console_overlay_;
   std::unique_ptr<ui::SettingsDialog> settings_overlay_;
   std::unique_ptr<ui::ImGuiDialog> achievements_overlay_;
diff --git a/include/rex/ui/overlay/debug_overlay.h b/include/rex/ui/overlay/debug_overlay.h
index e15e410..c548be1 100644
--- a/include/rex/ui/overlay/debug_overlay.h
+++ b/include/rex/ui/overlay/debug_overlay.h
@@ -10,6 +10,7 @@
  *              See LICENSE file in the project root for full license text.
  */
 #pragma once
+#include <string_view>
 #include <rex/ui/imgui_dialog.h>
 #include <array>
 #include <cstdint>
@@ -17,6 +18,10 @@
 
 namespace rex::ui {
 
+// DP1 2026-09-06: lets the app replace the SDK build stamp watermark shown by the
+// debug overlay (F3) with its own build string. Empty = REXGLUE_BUILD_STAMP.
+void SetDebugOverlayBuildStamp(std::string_view stamp);
+
 struct FrameStats {
   double frame_time_ms = 0;
   double fps = 0;
diff --git a/include/rex/ui/virtual_key.h b/include/rex/ui/virtual_key.h
index 3e1b948..a6286ce 100644
--- a/include/rex/ui/virtual_key.h
+++ b/include/rex/ui/virtual_key.h
@@ -42,6 +42,14 @@ enum class VirtualKey : uint16_t {
   kBack = 0x08,  // Backspace.
   kTab = 0x09,
 
+  // DP1 port 2026-09-05 (from dpour fork ce7d25d, include/rex/ui/virtual_key.h)
+  // Synthetic pulse keys used by MnK to expose vertical mouse-wheel scroll
+  // as bindable buttons (one logical "press" per WHEEL_DELTA detent of 120).
+  // These never come from real keyboard events; 0x0A/0x0B are unassigned in
+  // the Win32 VK table.
+  kWheelUp = 0x0A,
+  kWheelDown = 0x0B,
+
   kClear = 0x0C,
   kReturn = 0x0D,  // Enter.
 
diff --git a/src/core/threading_win.cpp b/src/core/threading_win.cpp
index 896dfec..123c093 100644
--- a/src/core/threading_win.cpp
+++ b/src/core/threading_win.cpp
@@ -93,12 +93,51 @@ void SyncMemory() {
   MemoryBarrier();
 }
 
+// DP1 2026-09-06 (DPRecomp #12): ::Sleep(ms) is quantised to the system timer
+// period (up to 15.6 ms unless somebody called timeBeginPeriod), so a guest
+// frame limiter that sleeps for "the rest of the 16.67 ms" overshoots by
+// whole milliseconds and the game runs below its target rate. Use a
+// high-resolution waitable timer (Windows 10 1803+, ~0.5 ms accuracy) when
+// the OS provides one and fall back to ::Sleep otherwise.
+#ifndef CREATE_WAITABLE_TIMER_HIGH_RESOLUTION
+#define CREATE_WAITABLE_TIMER_HIGH_RESOLUTION 0x00000002
+#endif
+
+namespace {
+struct HighResolutionSleepTimer {
+  HANDLE handle = nullptr;
+  bool tried = false;
+  HANDLE Get() {
+    if (!tried) {
+      tried = true;
+      handle = CreateWaitableTimerExW(nullptr, nullptr, CREATE_WAITABLE_TIMER_HIGH_RESOLUTION,
+                                      TIMER_ALL_ACCESS);
+    }
+    return handle;
+  }
+  ~HighResolutionSleepTimer() {
+    if (handle) {
+      CloseHandle(handle);
+    }
+  }
+};
+}  // namespace
+
 void Sleep(std::chrono::microseconds duration) {
   if (duration.count() < 100) {
     MaybeYield();
-  } else {
-    ::Sleep(static_cast<DWORD>(duration.count() / 1000));
+    return;
+  }
+  thread_local HighResolutionSleepTimer timer;
+  if (HANDLE h = timer.Get()) {
+    LARGE_INTEGER due;
+    due.QuadPart = -static_cast<LONGLONG>(duration.count()) * 10;  // 100 ns units, relative
+    if (SetWaitableTimer(h, &due, 0, nullptr, nullptr, FALSE)) {
+      WaitForSingleObject(h, INFINITE);
+      return;
+    }
   }
+  ::Sleep(static_cast<DWORD>(duration.count() / 1000));
 }
 
 SleepResult AlertableSleep(std::chrono::microseconds duration) {
diff --git a/src/graphics/CMakeLists.txt b/src/graphics/CMakeLists.txt
index 0d8f8af..e2f9b5e 100644
--- a/src/graphics/CMakeLists.txt
+++ b/src/graphics/CMakeLists.txt
@@ -25,6 +25,7 @@ set(REXGPU_XENOS_SOURCES
     pipeline/render_target/cache.cpp
     shared_memory.cpp
     pipeline/texture/cache.cpp
+    pipeline/texture/replacement.cpp
     pipeline/shader/interpreter.cpp
     pipeline/shader/translator.cpp
     pipeline/shader/translator_disasm.cpp
@@ -107,6 +108,7 @@ set_target_properties(rexgpu-xenos PROPERTIES
 
 target_include_directories(rexgpu-xenos PRIVATE
     ${REXGLUE_ROOT}/thirdparty/renderdoc
+    ${REXGLUE_ROOT}/thirdparty/stb  # stb_image.h for texture replacement PNGs
     ${REXGLUE_ROOT}  # For thirdparty/dxbc/DXBCChecksum.h include
 )
 
diff --git a/src/graphics/d3d12/command_processor.cpp b/src/graphics/d3d12/command_processor.cpp
index 19874a5..81706d6 100644
--- a/src/graphics/d3d12/command_processor.cpp
+++ b/src/graphics/d3d12/command_processor.cpp
@@ -10,6 +10,8 @@
  */
 
 #include <algorithm>
+#include <chrono>
+#include <thread>
 #include <cstdarg>
 #include <cstring>
 #include <sstream>
@@ -2400,7 +2402,25 @@ bool D3D12CommandProcessor::IssueDraw(xenos::PrimitiveType primitive_type, uint3
   }
   if (REXCVAR_GET(async_shader_compilation) &&
       pipeline_cache_->GetD3D12PipelineByHandle(pipeline_handle) == nullptr) {
-    return true;
+    // DP1 2026-09-06 (ported from the Downpour SDK fork): the PSO is still
+    // compiling on a background thread. pso_missing_policy=block spin-waits
+    // up to d3d12_pso_block_per_draw_budget_ms for it, so fast compiles never
+    // drop geometry; on timeout (or with policy=skip) the draw is dropped for
+    // this frame only.
+    if (REXCVAR_GET(pso_missing_policy) == "block") {
+      const auto budget = std::chrono::microseconds(
+          static_cast<int64_t>(REXCVAR_GET(d3d12_pso_block_per_draw_budget_ms) * 1000.0));
+      const auto wait_start = std::chrono::steady_clock::now();
+      while (pipeline_cache_->GetD3D12PipelineByHandle(pipeline_handle) == nullptr) {
+        std::this_thread::sleep_for(std::chrono::microseconds(50));
+        if (std::chrono::steady_clock::now() - wait_start > budget) {
+          return true;
+        }
+      }
+      // Fall through with the now-ready PSO.
+    } else {
+      return true;
+    }
   }
 
   // Update the textures - this may bind pipelines.
diff --git a/src/graphics/d3d12/pipeline_cache.cpp b/src/graphics/d3d12/pipeline_cache.cpp
index 8e6b854..8ff3824 100644
--- a/src/graphics/d3d12/pipeline_cache.cpp
+++ b/src/graphics/d3d12/pipeline_cache.cpp
@@ -11,6 +11,7 @@
 
 #include "thirdparty/dxbc/DXBCChecksum.h"
 
+#include <chrono>
 #include <algorithm>
 #include <atomic>
 #include <cinttypes>
@@ -32,6 +33,8 @@
 #include <rex/filesystem.h>
 #include <rex/graphics/d3d12/command_processor.h>
 #include <rex/graphics/d3d12/pipeline_cache.h>
+
+#include <rex/ui/overlay/pso_compile_indicator_state.h>
 #include <rex/graphics/d3d12/render_target_cache.h>
 #include <rex/graphics/flags.h>
 #include <rex/graphics/format/dxbc.h>
@@ -59,6 +62,34 @@ REXCVAR_DEFINE_INT32(d3d12_pipeline_creation_threads, -1, "GPU/D3D12",
     .range(-1, 32)
     .lifecycle(rex::cvar::Lifecycle::kRequiresRestart);
 
+// DP1 2026-09-06: ported from the Downpour SDK fork (pipeline_cache.cpp there).
+// Behavior when a draw call's PSO is still being compiled on a background
+// thread: 'skip' (draw dropped, geometry invisible this frame = pop-in),
+// 'block' (render thread waits up to d3d12_pso_block_per_draw_budget_ms for
+// that PSO, geometry never lost for fast compiles), 'sync' (no async compile
+// at all, every miss compiles inline on the render thread).
+REXCVAR_DEFINE_BOOL(d3d12_pso_library_enable, true, "GPU/D3D12",
+                    "DP1 v1.1 (Downpour port): keep driver-compiled PSO blobs across runs in an "
+                    "ID3D12PipelineLibrary file (cache/shaders/local/*.pso_lib). Warm launches "
+                    "skip the driver compile per pipeline. GPU/driver specific; rebuilt "
+                    "automatically after a driver or adapter change.")
+    .lifecycle(rex::cvar::Lifecycle::kRequiresRestart);
+
+REXCVAR_DEFINE_STRING(pso_missing_policy, "block", "GPU",
+                      "PSO still compiling: skip (drop draw), block (wait per draw, budgeted), "
+                      "sync (compile inline)")
+    .allowed({"skip", "block", "sync"})
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_DOUBLE(d3d12_pso_block_per_draw_budget_ms, 8.0, "GPU/D3D12",
+                      "Per-draw wait budget (ms) when pso_missing_policy=block; on timeout the "
+                      "draw is skipped for this frame")
+    .range(0.5, 500.0)
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_BOOL(d3d12_pso_no_block_at_submission_end, true, "GPU/D3D12",
+                    "Do not stall the render thread at frame end waiting for in-flight "
+                    "background PSO compiles (they roll over to later frames)")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+
 REXCVAR_DEFINE_BOOL(d3d12_tessellation_wireframe, false, "GPU/D3D12",
                     "Render tessellation as wireframe");
 
@@ -260,6 +291,61 @@ void PipelineCache::InitializeShaderStorage(const std::filesystem::path& cache_r
     return;
   }
   pipeline_storage_file_flush_needed_ = false;
+
+  // DP1 v1.1: ID3D12PipelineLibrary blob cache (see pipeline_cache.h).
+  pipeline_library_.Reset();
+  pipeline_library_blob_.clear();
+  pipeline_library_path_.clear();
+  pipeline_library_load_hits_.store(0, std::memory_order_relaxed);
+  pipeline_library_store_count_.store(0, std::memory_order_relaxed);
+  pipeline_library_dirty_.store(false, std::memory_order_relaxed);
+  if (REXCVAR_GET(d3d12_pso_library_enable)) {
+    ID3D12Device* library_device = command_processor_.GetD3D12Provider().GetDevice();
+    Microsoft::WRL::ComPtr<ID3D12Device1> device1;
+    if (SUCCEEDED(library_device->QueryInterface(IID_PPV_ARGS(&device1)))) {
+      auto shader_storage_local_root = shader_storage_root / "local";
+      std::error_code ec;
+      std::filesystem::create_directories(shader_storage_local_root, ec);
+      pipeline_library_path_ =
+          shader_storage_local_root /
+          fmt::format("{:08X}.{}.d3d12.pso_lib", title_id, edram_rov_used ? "rov" : "rtv");
+      FILE* lib_file = rex::filesystem::OpenFile(pipeline_library_path_, "rb");
+      if (lib_file) {
+        rex::filesystem::Seek(lib_file, 0, SEEK_END);
+        int64_t size = rex::filesystem::Tell(lib_file);
+        if (size > 0 && size < int64_t(1) << 30) {
+          pipeline_library_blob_.resize(size_t(size));
+          rex::filesystem::Seek(lib_file, 0, SEEK_SET);
+          if (fread(pipeline_library_blob_.data(), 1, pipeline_library_blob_.size(), lib_file) !=
+              pipeline_library_blob_.size()) {
+            pipeline_library_blob_.clear();
+          }
+        }
+        fclose(lib_file);
+      }
+      HRESULT hr = device1->CreatePipelineLibrary(
+          pipeline_library_blob_.empty() ? nullptr : pipeline_library_blob_.data(),
+          pipeline_library_blob_.size(), IID_PPV_ARGS(&pipeline_library_));
+      if (FAILED(hr)) {
+        // E_INVALIDARG / D3D12_ERROR_DRIVER_VERSION_MISMATCH /
+        // D3D12_ERROR_ADAPTER_NOT_FOUND: start over with an empty library.
+        REXGPU_INFO("PSO library blob unusable (HRESULT 0x{:08X}), rebuilding from scratch",
+                    uint32_t(hr));
+        pipeline_library_blob_.clear();
+        hr = device1->CreatePipelineLibrary(nullptr, 0, IID_PPV_ARGS(&pipeline_library_));
+        if (FAILED(hr)) {
+          REXGPU_INFO("PSO library disabled (CreatePipelineLibrary failed 0x{:08X})",
+                      uint32_t(hr));
+          pipeline_library_.Reset();
+        } else {
+          pipeline_library_dirty_.store(true, std::memory_order_relaxed);
+        }
+      } else {
+        REXGPU_INFO("PSO library loaded: {} bytes from '{}'", pipeline_library_blob_.size(),
+                    rex::path_to_utf8(pipeline_library_path_));
+      }
+    }
+  }
   // 'XEPS'.
   const uint32_t pipeline_storage_magic = 0x53504558;
   // 'DXRO' or 'DXRT'.
@@ -490,6 +576,8 @@ void PipelineCache::InitializeShaderStorage(const std::filesystem::path& cache_r
       }
       shaders_translation_thread_cond.notify_one();
       ++shaders_translated;
+      PsoCompileIndicatorStateRef().storage_shaders_translated.store(
+          uint32_t(shaders_translated), std::memory_order_relaxed);
     }
     if (!shader_translation_threads.empty()) {
       {
@@ -514,6 +602,10 @@ void PipelineCache::InitializeShaderStorage(const std::filesystem::path& cache_r
                 (rex::chrono::Clock::QueryHostTickCount() - shader_storage_initialization_start) *
                     1000 / rex::chrono::Clock::QueryHostTickFrequency());
     rex::filesystem::TruncateStdioFile(shader_storage_file_, shader_storage_valid_bytes);
+    // DP1 v1.1: the stream was in read mode at EOF; C requires a positioning
+    // call before switching to writing, otherwise the storage writer thread fwrite
+    // silently fails and the storage never grows past its first session.
+    rex::filesystem::Seek(shader_storage_file_, 0, SEEK_END);
   } else {
     rex::filesystem::TruncateStdioFile(shader_storage_file_, 0);
     shader_storage_file_header.magic = shader_storage_magic;
@@ -542,6 +634,8 @@ void PipelineCache::InitializeShaderStorage(const std::filesystem::path& cache_r
     }
 
     size_t pipelines_created = 0;
+    PsoCompileIndicatorStateRef().storage_pipelines_total.store(
+        uint32_t(pipeline_stored_descriptions.size()), std::memory_order_release);
     for (const PipelineStoredDescription& pipeline_stored_description :
          pipeline_stored_descriptions) {
       const PipelineDescription& pipeline_description = pipeline_stored_description.description;
@@ -641,6 +735,8 @@ void PipelineCache::InitializeShaderStorage(const std::filesystem::path& cache_r
         {
           std::lock_guard<std::mutex> lock(creation_request_lock_);
           creation_queue_.push(new_pipeline);
+          PsoCompileIndicatorStateRef().creation_queue_size.store(
+              uint32_t(creation_queue_.size()), std::memory_order_relaxed);
         }
         creation_request_cond_.notify_one();
       } else {
@@ -709,6 +805,13 @@ void PipelineCache::InitializeShaderStorage(const std::filesystem::path& cache_r
   shader_storage_cache_root_ = cache_root;
   shader_storage_title_id_ = title_id;
 
+  // DP1 v1.1: same read->write switch for the pipeline description stream.
+  if (pipeline_storage_file_) {
+    rex::filesystem::Seek(pipeline_storage_file_, 0, SEEK_END);
+  }
+  // Capture the replayed PSOs even if the process is killed later.
+  FlushPipelineLibraryToDisk();
+
   // Start the storage writing thread.
   storage_write_flush_shaders_ = false;
   storage_write_flush_pipelines_ = false;
@@ -731,6 +834,12 @@ void PipelineCache::ShutdownShaderStorage() {
   storage_write_shader_queue_.clear();
   storage_write_pipeline_queue_.clear();
 
+  FlushPipelineLibraryToDisk();
+  pipeline_library_.Reset();
+  pipeline_library_blob_.clear();
+  pipeline_library_path_.clear();
+  pipeline_library_dirty_.store(false, std::memory_order_relaxed);
+
   if (pipeline_storage_file_) {
     fclose(pipeline_storage_file_);
     pipeline_storage_file_ = nullptr;
@@ -771,7 +880,10 @@ void PipelineCache::EndSubmission() {
       // Assuming the creation queue is already empty (because the processor
       // thread also worked on creating the leftover pipelines), so only check
       // if there are threads with pipelines currently being created.
-      await_creation_completion_event = creation_threads_busy_ != 0;
+      await_creation_completion_event = 
+          // DP1: with d3d12_pso_no_block_at_submission_end the frame submits
+          // immediately and pending PSOs are picked up by later frames.
+          !REXCVAR_GET(d3d12_pso_no_block_at_submission_end) && creation_threads_busy_ != 0;
       if (await_creation_completion_event) {
         creation_completion_event_->Reset();
         creation_completion_set_event_ = true;
@@ -910,7 +1022,9 @@ bool PipelineCache::ConfigurePipeline(
   assert_not_null(root_signature_out);
 
   bool use_async = REXCVAR_GET(async_shader_compilation) && !creation_threads_.empty() &&
-                   pixel_shader != nullptr;
+                   pixel_shader != nullptr &&
+                   // DP1: pso_missing_policy=sync forces inline compiles.
+                   REXCVAR_GET(pso_missing_policy) != "sync";
 
   // Ensure shaders are translated - needed now for GetCurrentStateDescription.
   // Edge flags are not supported yet (because polygon primitives are not).
@@ -1015,6 +1129,7 @@ bool PipelineCache::ConfigurePipeline(
   pipelines_.emplace(hash, new_pipeline);
   COUNT_profile_set("gpu/pipeline_cache/pipelines", pipelines_.size());
 
+  PsoCompileIndicatorStateRef().total_misses_session.fetch_add(1, std::memory_order_relaxed);
   if (use_async) {
     uint32_t bound_rts =
         pipeline_util::GetBoundRTMaskFromNormalizedColorMask(normalized_color_mask);
@@ -1030,10 +1145,26 @@ bool PipelineCache::ConfigurePipeline(
     {
       std::lock_guard<std::mutex> lock(creation_request_lock_);
       creation_queue_.push(new_pipeline);
+      PsoCompileIndicatorStateRef().creation_queue_size.store(
+          uint32_t(creation_queue_.size()), std::memory_order_relaxed);
     }
     creation_request_cond_.notify_one();
   } else {
+    // Inline (draw-blocking) compile: report it so the indicator can flash
+    // "Shader stall N ms" afterwards.
+    const auto sync_start = std::chrono::steady_clock::now();
     new_pipeline->state.store(CreateD3D12Pipeline(runtime_description), std::memory_order_release);
+    const auto sync_end = std::chrono::steady_clock::now();
+    auto& indicator = PsoCompileIndicatorStateRef();
+    indicator.sync_compiles_session.fetch_add(1, std::memory_order_relaxed);
+    indicator.last_sync_compile_steady_ns.store(
+        uint64_t(std::chrono::duration_cast<std::chrono::nanoseconds>(sync_end.time_since_epoch())
+                     .count()),
+        std::memory_order_relaxed);
+    const float sync_ms = std::chrono::duration<float, std::milli>(sync_end - sync_start).count();
+    uint32_t sync_ms_bits;
+    std::memcpy(&sync_ms_bits, &sync_ms, sizeof(sync_ms_bits));
+    indicator.last_sync_compile_duration_ms_bits.store(sync_ms_bits, std::memory_order_relaxed);
   }
 
   if (pipeline_storage_file_) {
@@ -3028,17 +3159,42 @@ ID3D12PipelineState* PipelineCache::CreateD3D12Pipeline(
 
   // Create the D3D12 pipeline state object.
   ID3D12Device* device = command_processor_.GetD3D12Provider().GetDevice();
-  ID3D12PipelineState* state;
-  if (FAILED(device->CreateGraphicsPipelineState(&state_desc, IID_PPV_ARGS(&state)))) {
-    if (runtime_description.pixel_shader != nullptr) {
-      REXGPU_ERROR("Failed to create graphics pipeline with VS {:016X}, PS {:016X}",
-                   runtime_description.vertex_shader->shader().ucode_data_hash(),
-                   runtime_description.pixel_shader->shader().ucode_data_hash());
+  ID3D12PipelineState* state = nullptr;
+  // DP1 v1.1: ID3D12PipelineLibrary fast path, keyed by the description hash
+  // (the same hash the .xpso storage uses).
+  wchar_t pipeline_name[17];
+  swprintf_s(pipeline_name, L"%016llX",
+             static_cast<unsigned long long>(XXH3_64bits(&description, sizeof(description))));
+  if (pipeline_library_) {
+    std::lock_guard<std::mutex> lock(pipeline_library_mutex_);
+    if (SUCCEEDED(pipeline_library_->LoadGraphicsPipeline(pipeline_name, &state_desc,
+                                                          IID_PPV_ARGS(&state)))) {
+      pipeline_library_load_hits_.fetch_add(1, std::memory_order_relaxed);
     } else {
-      REXGPU_ERROR("Failed to create graphics pipeline with VS {:016X}",
-                   runtime_description.vertex_shader->shader().ucode_data_hash());
+      state = nullptr;
+    }
+  }
+  if (!state) {
+    if (FAILED(device->CreateGraphicsPipelineState(&state_desc, IID_PPV_ARGS(&state)))) {
+      if (runtime_description.pixel_shader != nullptr) {
+        REXGPU_ERROR("Failed to create graphics pipeline with VS {:016X}, PS {:016X}",
+                     runtime_description.vertex_shader->shader().ucode_data_hash(),
+                     runtime_description.pixel_shader->shader().ucode_data_hash());
+      } else {
+        REXGPU_ERROR("Failed to create graphics pipeline with VS {:016X}",
+                     runtime_description.vertex_shader->shader().ucode_data_hash());
+      }
+      return nullptr;
+    }
+    if (pipeline_library_) {
+      std::lock_guard<std::mutex> lock(pipeline_library_mutex_);
+      if (SUCCEEDED(pipeline_library_->StorePipeline(pipeline_name, state))) {
+        pipeline_library_store_count_.fetch_add(1, std::memory_order_relaxed);
+        pipeline_library_dirty_.store(true, std::memory_order_relaxed);
+        PsoCompileIndicatorStateRef().library_stores_session.fetch_add(1,
+                                                                        std::memory_order_relaxed);
+      }
     }
-    return nullptr;
   }
   std::u16string name;
   if (runtime_description.pixel_shader != nullptr) {
@@ -3053,6 +3209,54 @@ ID3D12PipelineState* PipelineCache::CreateD3D12Pipeline(
   return state;
 }
 
+// DP1 v1.1: serialize the pipeline library to disk (atomic replace). No-op
+// when nothing new was stored since the last flush.
+void PipelineCache::FlushPipelineLibraryToDisk() {
+  if (!pipeline_library_ || !pipeline_library_dirty_.load(std::memory_order_relaxed) ||
+      pipeline_library_path_.empty()) {
+    return;
+  }
+  std::vector<uint8_t> serialized;
+  HRESULT hr;
+  {
+    std::lock_guard<std::mutex> lock(pipeline_library_mutex_);
+    SIZE_T serialized_size = pipeline_library_->GetSerializedSize();
+    if (serialized_size == 0) {
+      return;
+    }
+    serialized.resize(serialized_size);
+    hr = pipeline_library_->Serialize(serialized.data(), serialized_size);
+  }
+  if (FAILED(hr)) {
+    REXGPU_WARN("PSO library Serialize failed: HRESULT 0x{:08X}", uint32_t(hr));
+    return;
+  }
+  auto tmp_path = pipeline_library_path_;
+  tmp_path += ".tmp";
+  FILE* lib_file = rex::filesystem::OpenFile(tmp_path, "wb");
+  if (!lib_file) {
+    REXGPU_WARN("PSO library file open failed: {}", rex::path_to_utf8(tmp_path));
+    return;
+  }
+  size_t written = fwrite(serialized.data(), 1, serialized.size(), lib_file);
+  fclose(lib_file);
+  std::error_code ec;
+  if (written != serialized.size()) {
+    std::filesystem::remove(tmp_path, ec);
+    return;
+  }
+  std::filesystem::remove(pipeline_library_path_, ec);
+  std::filesystem::rename(tmp_path, pipeline_library_path_, ec);
+  if (ec) {
+    REXGPU_WARN("PSO library rename failed: {}", ec.message());
+    return;
+  }
+  pipeline_library_dirty_.store(false, std::memory_order_relaxed);
+  REXGPU_INFO("PSO library saved: {} bytes, {} loads / {} stores this session",
+              serialized.size(), pipeline_library_load_hits_.load(std::memory_order_relaxed),
+              pipeline_library_store_count_.load(std::memory_order_relaxed));
+}
+
 void PipelineCache::StorageWriteThread() {
   ShaderStoredHeader shader_header;
   // Don't leak anything in unused bits.
@@ -3223,6 +3427,10 @@ void PipelineCache::CreationThread(size_t thread_index) {
       pipeline_to_create = creation_queue_.top();
       creation_queue_.pop();
       ++creation_threads_busy_;
+      PsoCompileIndicatorStateRef().creation_queue_size.store(uint32_t(creation_queue_.size()),
+                                                              std::memory_order_relaxed);
+      PsoCompileIndicatorStateRef().creation_threads_busy.store(
+          uint32_t(creation_threads_busy_), std::memory_order_relaxed);
     }
 
     PipelineRuntimeDescription runtime_description;
@@ -3236,9 +3444,21 @@ void PipelineCache::CreationThread(size_t thread_index) {
     // Pipeline created - the thread is not busy anymore, safe to set the
     // completion event if needed (at the next iteration, or in some other
     // thread).
+    {
+      auto& indicator = PsoCompileIndicatorStateRef();
+      indicator.total_completions_session.fetch_add(1, std::memory_order_relaxed);
+      indicator.storage_pipelines_created.fetch_add(1, std::memory_order_relaxed);
+      indicator.last_completion_steady_ns.store(
+          uint64_t(std::chrono::duration_cast<std::chrono::nanoseconds>(
+                       std::chrono::steady_clock::now().time_since_epoch())
+                       .count()),
+          std::memory_order_relaxed);
+    }
     {
       std::lock_guard<std::mutex> lock(creation_request_lock_);
       --creation_threads_busy_;
+      PsoCompileIndicatorStateRef().creation_threads_busy.store(
+          uint32_t(creation_threads_busy_), std::memory_order_relaxed);
     }
   }
 }
@@ -3262,6 +3482,14 @@ void PipelineCache::CreateQueuedPipelinesOnProcessorThread() {
     }
     pipeline_to_create->state.store(CreateD3D12Pipeline(runtime_description),
                                     std::memory_order_release);
+    {
+      auto& indicator = PsoCompileIndicatorStateRef();
+      indicator.total_completions_session.fetch_add(1, std::memory_order_relaxed);
+      indicator.storage_pipelines_created.fetch_add(1, std::memory_order_relaxed);
+      std::lock_guard<std::mutex> lock(creation_request_lock_);
+      indicator.creation_queue_size.store(uint32_t(creation_queue_.size()),
+                                          std::memory_order_relaxed);
+    }
   }
 }
 
diff --git a/src/graphics/d3d12/render_target_cache.cpp b/src/graphics/d3d12/render_target_cache.cpp
index dd321a5..4e3076c 100644
--- a/src/graphics/d3d12/render_target_cache.cpp
+++ b/src/graphics/d3d12/render_target_cache.cpp
@@ -79,6 +79,9 @@ namespace shaders {
 #include "../shaders/bytecode/d3d12_5_1/resolve_full_64bpp_scaled_cs.h"
 #include "../shaders/bytecode/d3d12_5_1/resolve_full_8bpp_cs.h"
 #include "../shaders/bytecode/d3d12_5_1/resolve_full_8bpp_scaled_cs.h"
+// DP1 fix (ROV 7e3 -> 8888), built with fxc from
+// ../shaders/edram_7e3_to_8888_cs.hlsl.
+#include "../shaders/bytecode/d3d12_5_1/edram_7e3_to_8888_cs.h"
 }  // namespace shaders
 
 const D3D12RenderTargetCache::ResolveCopyShaderCode
@@ -948,6 +951,19 @@ bool D3D12RenderTargetCache::Initialize() {
       return false;
     }
     resolve_rov_clear_64bpp_pipeline_->SetName(L"Resolve Clear 64bpp");
+
+    // DP1 fix: 7e3 -> 8888 in-place EDRAM conversion pipeline.
+    rov_7e3_to_8888_pipeline_ = ui::d3d12::util::CreateComputePipeline(
+        device, shaders::edram_7e3_to_8888_cs, sizeof(shaders::edram_7e3_to_8888_cs),
+        resolve_rov_clear_root_signature_);
+    if (rov_7e3_to_8888_pipeline_ == nullptr) {
+      REXGPU_ERROR(
+          "D3D12RenderTargetCache: Failed to create the EDRAM 7e3 -> 8888 "
+          "conversion pipeline");
+      Shutdown();
+      return false;
+    }
+    rov_7e3_to_8888_pipeline_->SetName(L"EDRAM 7e3 to 8888");
   } else {
     assert_unhandled_case(path_);
     Shutdown();
@@ -960,6 +976,7 @@ bool D3D12RenderTargetCache::Initialize() {
 }
 
 void D3D12RenderTargetCache::Shutdown(bool from_destructor) {
+  ui::d3d12::util::ReleaseAndNull(rov_7e3_to_8888_pipeline_);
   ui::d3d12::util::ReleaseAndNull(resolve_rov_clear_64bpp_pipeline_);
   ui::d3d12::util::ReleaseAndNull(resolve_rov_clear_32bpp_pipeline_);
   ui::d3d12::util::ReleaseAndNull(resolve_rov_clear_root_signature_);
@@ -1370,6 +1387,9 @@ bool D3D12RenderTargetCache::Resolve(const memory::Memory& memory, D3D12SharedMe
           // instance), overlap of the depth and the color ranges is highly
           // unlikely.
           CommitEdramBufferUAVWrites();
+          // DP1 fix: the clear overwrites the tiles - record their new format
+          // class (no conversion needed).
+          PixelShaderInterlockTrackClear(resolve_info, clear_depth, clear_color);
           command_list.D3DSetComputeRootSignature(resolve_rov_clear_root_signature_);
           command_list.D3DSetComputeRootDescriptorTable(1, descriptor_edram.second);
           std::pair<uint32_t, uint32_t> clear_group_count = resolve_info.GetClearShaderGroupCount(
@@ -1716,6 +1736,71 @@ void D3D12RenderTargetCache::RequestPixelShaderInterlockBarrier() {
   CommitEdramBufferUAVWrites();
 }
 
+void D3D12RenderTargetCache::PixelShaderInterlockReinterpret7e3To8888(uint32_t start_tiles,
+                                                                      uint32_t length_tiles) {
+  // DP1 fix: see RenderTargetCache::PixelShaderInterlockTrackTiles.
+  assert_true(GetPath() == Path::kPixelShaderInterlock);
+  if (!rov_7e3_to_8888_pipeline_ || !length_tiles ||
+      start_tiles + length_tiles > xenos::kEdramTileCount) {
+    return;
+  }
+  ui::d3d12::util::DescriptorCpuGpuHandlePair descriptor_edram;
+  if (bindless_resources_used_) {
+    descriptor_edram = command_processor_.GetSystemBindlessViewHandlePair(
+        D3D12CommandProcessor::SystemBindlessView::kEdramR32G32B32A32UintUAV);
+  } else {
+    if (!command_processor_.RequestOneUseSingleViewDescriptors(1, &descriptor_edram)) {
+      return;
+    }
+    WriteEdramUintPow2UAVDescriptor(descriptor_edram.first, 4);
+  }
+  DeferredCommandList& command_list = command_processor_.GetDeferredCommandList();
+  TransitionEdramBuffer(D3D12_RESOURCE_STATE_UNORDERED_ACCESS);
+  // All preceding interlocked (and UAV) writes must be visible to the
+  // conversion, which reads and writes the same dwords.
+  CommitEdramBufferUAVWrites();
+  // 32bpp tiles are contiguous kEdramTileWidthSamples * kEdramTileHeightSamples
+  // * resolution scale dwords in the ROV EDRAM buffer (see
+  // edram_32bpp_tile_pitch_dwords_scaled), and the shader works on uint4s.
+  uint32_t tile_uint4s = xenos::kEdramTileWidthSamples * xenos::kEdramTileHeightSamples *
+                         (draw_resolution_scale_x() * draw_resolution_scale_y()) / 4;
+  uint32_t constants[2] = {start_tiles * tile_uint4s, length_tiles * tile_uint4s};
+  command_list.D3DSetComputeRootSignature(resolve_rov_clear_root_signature_);
+  command_list.D3DSetComputeRoot32BitConstants(0, uint32_t(rex::countof(constants)), constants,
+                                               0);
+  command_list.D3DSetComputeRootDescriptorTable(1, descriptor_edram.second);
+  command_processor_.SetExternalPipeline(rov_7e3_to_8888_pipeline_);
+  command_processor_.SubmitBarriers();
+  // 64 threads x 4 uint4s per group - keep in sync with the shader.
+  constexpr uint32_t kUint4sPerGroup = 64 * 4;
+  command_list.D3DDispatch((constants[1] + kUint4sPerGroup - 1) / kUint4sPerGroup, 1, 1);
+  // The draw that follows must wait for the conversion.
+  MarkEdramBufferModified(EdramBufferModificationStatus::kAsUAV);
+}
+
+void D3D12RenderTargetCache::PixelShaderInterlockTrackClear(
+    const draw_util::ResolveInfo& resolve_info, bool clear_depth, bool clear_color) {
+  // DP1 fix: mark the cleared tiles (row by row - the resolve area is a
+  // rectangle within the pitch) with the format class of the clear.
+  for (uint32_t i = 0; i < 2; ++i) {
+    bool is_color = i != 0;
+    if (is_color ? !clear_color : !clear_depth) {
+      continue;
+    }
+    const draw_util::ResolveEdramInfo& edram_info =
+        is_color ? resolve_info.color_edram_info : resolve_info.depth_edram_info;
+    uint32_t base_tiles, row_length_used_tiles, rows;
+    draw_util::GetResolveEdramTileSpan(edram_info, resolve_info.coordinate_info,
+                                       resolve_info.height_div_8, base_tiles,
+                                       row_length_used_tiles, rows);
+    for (uint32_t row = 0; row < rows; ++row) {
+      PixelShaderInterlockTrackTiles(base_tiles + row * edram_info.pitch_tiles,
+                                     row_length_used_tiles, is_color,
+                                     xenos::ColorRenderTargetFormat(edram_info.format), false);
+    }
+  }
+}
+
 void D3D12RenderTargetCache::TransitionEdramBuffer(D3D12_RESOURCE_STATES new_state) {
   if (command_processor_.PushTransitionBarrier(edram_buffer_, edram_buffer_state_, new_state)) {
     // Resetting edram_buffer_modification_status_ only if the barrier has been
@@ -3126,6 +3211,20 @@ ID3D12PipelineState* const* D3D12RenderTargetCache::GetOrCreateTransferPipelines
                       dest_color_format ==
                           xenos::ColorRenderTargetFormat::k_2_10_10_10_FLOAT_AS_16_16_16_16)) {
             a.OpMov(dxbc::Dest::O(0), dxbc::Src::R(1));
+          } else if (dest_is_color &&
+                     (dest_color_format == xenos::ColorRenderTargetFormat::k_8_8_8_8 ||
+                      dest_color_format ==
+                          xenos::ColorRenderTargetFormat::k_8_8_8_8_GAMMA)) {
+            // DP1 fix (LittleBitUA, 2026-06-20, re-applied on nightly 0c7b01a
+            // 2026-09-05): HDR 7e3 -> 8888 ownership transfer with
+            // value-clipping instead of bit-preservation. Bit-preserving is
+            // Xenos-faithful, but the raw 7e3 bits unpacked as UNORM8 leak
+            // through later tonemap + alpha-blend passes wherever src.a == 0
+            // (alpha-tested hair, foliage, statue billboards in Deadly
+            // Premonition) as rainbow speckles. Saturating the HDR floats to
+            // [0, 1] and writing them as UNORM8 leaves clipped-HDR residue at
+            // those pixels, visually indistinguishable from the tonemap output.
+            a.OpMov(dxbc::Dest::O(0), dxbc::Src::R(1), true);
           } else {
             color_packed_in_r1x = true;
             // Float16 has a wider range for both color and alpha, also NaNs -
diff --git a/src/graphics/d3d12/texture_cache.cpp b/src/graphics/d3d12/texture_cache.cpp
index 2be0e8e..a7016ea 100644
--- a/src/graphics/d3d12/texture_cache.cpp
+++ b/src/graphics/d3d12/texture_cache.cpp
@@ -15,6 +15,8 @@
 #include <cstddef>
 #include <cstring>
 #include <memory>
+#include <mutex>
+#include <set>
 #include <utility>
 
 #include <rex/assert.h>
@@ -988,6 +990,27 @@ D3D12TextureCache::SamplerParameters D3D12TextureCache::GetSamplerParameters(
   }
   parameters.mip_base_map = mip_base_map;
 
+  // DP1 diagnostics (texture_dump = true): every sampler that is not fully
+  // linear is logged once per (texture, parameters) so point-filtered
+  // textures can be matched with the dumped PNGs by guest address.
+  if (REXCVAR_GET(texture_dump) && !(parameters.mag_linear && parameters.min_linear)) {
+    static std::mutex seen_mutex;
+    static std::set<uint64_t> seen;
+    const uint64_t seen_key = (uint64_t(fetch.base_address) << 32) | parameters.value;
+    std::lock_guard<std::mutex> lock(seen_mutex);
+    if (seen.insert(seen_key).second) {
+      uint32_t w1 = 0, h1 = 0, d1 = 0, bp = 0, mp = 0;
+      texture_util::GetSubresourcesFromFetchConstant(fetch, &w1, &h1, &d1, &bp, &mp, nullptr, nullptr);
+      const FormatInfo* fi = FormatInfo::Get(fetch.format);
+      REXGPU_INFO("Sampler non-linear: fetch#{} base {:08X} {}x{} {} mag={} min={} mip={} "
+                  "(shader mag={} min={} mip={}) aniso={} mips {}..{}",
+                  binding.fetch_constant, bp << 12, w1 + 1, h1 + 1, fi ? fi->name : "?",
+                  uint32_t(mag_filter), uint32_t(min_filter), uint32_t(mip_filter),
+                  uint32_t(binding.mag_filter), uint32_t(binding.min_filter),
+                  uint32_t(binding.mip_filter), uint32_t(aniso_filter), mip_min_level, mip_max_level);
+    }
+  }
+
   return parameters;
 }
 
@@ -1523,6 +1546,16 @@ std::unique_ptr<TextureCache::Texture> D3D12TextureCache::CreateTexture(TextureK
     unsupported_format_features_used_[uint32_t(key.format)] |= kUnsupportedResourceBit;
     return nullptr;
   }
+  // Texture replacement: a host PNG stands in for the guest texture.
+  const texture_replacement::Image* replacement = nullptr;
+  uint32_t replacement_mip_count = 0;
+  if (creating_data_hash_ && key.dimension == xenos::DataDimension::k2DOrStacked &&
+      key.GetDepthOrArraySize() == 1 && !key.scaled_resolve && !key.signed_separate) {
+    replacement = texture_replacement::Registry::Get().Find(creating_data_hash_, &creating_view_);
+    if (replacement) {
+      replacement_mip_count = std::min(uint32_t(replacement->levels.size()), uint32_t(key.mip_max_level + 1));
+    }
+  }
   if (key.dimension == xenos::DataDimension::k3D) {
     desc.Dimension = D3D12_RESOURCE_DIMENSION_TEXTURE3D;
   } else {
@@ -1538,6 +1571,12 @@ std::unique_ptr<TextureCache::Texture> D3D12TextureCache::CreateTexture(TextureK
   }
   desc.DepthOrArraySize = key.GetDepthOrArraySize();
   desc.MipLevels = key.mip_max_level + 1;
+  if (replacement) {
+    desc.Format = DXGI_FORMAT_R8G8B8A8_UNORM;
+    desc.Width = replacement->width;
+    desc.Height = replacement->height;
+    desc.MipLevels = replacement_mip_count;
+  }
   desc.SampleDesc.Count = 1;
   desc.SampleDesc.Quality = 0;
   desc.Layout = D3D12_TEXTURE_LAYOUT_UNKNOWN;
@@ -1554,13 +1593,103 @@ std::unique_ptr<TextureCache::Texture> D3D12TextureCache::CreateTexture(TextureK
                                              resource_state, nullptr, IID_PPV_ARGS(&resource)))) {
     return nullptr;
   }
-  return std::unique_ptr<Texture>(new D3D12Texture(*this, key, resource.Get(), resource_state));
+  D3D12Texture* texture = new D3D12Texture(*this, key, resource.Get(), resource_state);
+  if (replacement) {
+    texture->SetReplacement(replacement, replacement_mip_count);
+    REXGPU_INFO("Texture replacement: {:016X} {}x{} -> host {}x{} ({} mips)", creating_data_hash_,
+                key.GetWidth(), key.GetHeight(), replacement->width, replacement->height,
+                replacement_mip_count);
+  }
+  return std::unique_ptr<Texture>(texture);
+}
+
+bool D3D12TextureCache::UploadReplacementTexture(D3D12Texture& texture) {
+  const texture_replacement::Image& image = *texture.replacement();
+  uint32_t mip_count = texture.replacement_mip_count();
+  ID3D12Device* device = command_processor_.GetD3D12Provider().GetDevice();
+  DeferredCommandList& command_list = command_processor_.GetDeferredCommandList();
+
+  // Placed footprints for every level in one upload buffer.
+  std::vector<D3D12_PLACED_SUBRESOURCE_FOOTPRINT> footprints(mip_count);
+  UINT64 total = 0;
+  for (uint32_t level = 0; level < mip_count; ++level) {
+    uint32_t w = image.LevelWidth(level), h = image.LevelHeight(level);
+    D3D12_PLACED_SUBRESOURCE_FOOTPRINT& fp = footprints[level];
+    fp.Offset = total;
+    fp.Footprint.Format = DXGI_FORMAT_R8G8B8A8_UNORM;
+    fp.Footprint.Width = w;
+    fp.Footprint.Height = h;
+    fp.Footprint.Depth = 1;
+    fp.Footprint.RowPitch = rex::align(w * 4, uint32_t(D3D12_TEXTURE_DATA_PITCH_ALIGNMENT));
+    total += rex::align(UINT64(fp.Footprint.RowPitch) * h,
+                        UINT64(D3D12_TEXTURE_DATA_PLACEMENT_ALIGNMENT));
+  }
+
+  // The upload buffer is created once per texture and kept (small UI
+  // textures; the contents never change).
+  Microsoft::WRL::ComPtr<ID3D12Resource>& upload = texture.replacement_upload();
+  if (!upload) {
+    D3D12_HEAP_PROPERTIES heap = {};
+    heap.Type = D3D12_HEAP_TYPE_UPLOAD;
+    D3D12_RESOURCE_DESC desc = {};
+    desc.Dimension = D3D12_RESOURCE_DIMENSION_BUFFER;
+    desc.Width = total;
+    desc.Height = 1;
+    desc.DepthOrArraySize = 1;
+    desc.MipLevels = 1;
+    desc.Format = DXGI_FORMAT_UNKNOWN;
+    desc.SampleDesc.Count = 1;
+    desc.Layout = D3D12_TEXTURE_LAYOUT_ROW_MAJOR;
+    if (FAILED(device->CreateCommittedResource(&heap, D3D12_HEAP_FLAG_NONE, &desc,
+                                               D3D12_RESOURCE_STATE_GENERIC_READ, nullptr,
+                                               IID_PPV_ARGS(&upload)))) {
+      REXGPU_ERROR("Texture replacement: failed to create the upload buffer");
+      return false;
+    }
+    void* mapped = nullptr;
+    D3D12_RANGE read_range = {0, 0};
+    if (FAILED(upload->Map(0, &read_range, &mapped))) {
+      upload.Reset();
+      return false;
+    }
+    for (uint32_t level = 0; level < mip_count; ++level) {
+      uint32_t w = image.LevelWidth(level), h = image.LevelHeight(level);
+      const std::vector<uint8_t>& src = image.levels[level];
+      uint8_t* dst = static_cast<uint8_t*>(mapped) + footprints[level].Offset;
+      for (uint32_t y = 0; y < h; ++y) {
+        std::memcpy(dst + size_t(y) * footprints[level].Footprint.RowPitch,
+                    src.data() + size_t(y) * w * 4, size_t(w) * 4);
+      }
+    }
+    upload->Unmap(0, nullptr);
+  }
+
+  texture.MarkAsUsed();
+  ID3D12Resource* resource = texture.resource();
+  command_processor_.PushTransitionBarrier(
+      resource, texture.SetResourceState(D3D12_RESOURCE_STATE_COPY_DEST),
+      D3D12_RESOURCE_STATE_COPY_DEST);
+  command_processor_.SubmitBarriers();
+  D3D12_TEXTURE_COPY_LOCATION location_source, location_dest;
+  location_source.pResource = upload.Get();
+  location_source.Type = D3D12_TEXTURE_COPY_TYPE_PLACED_FOOTPRINT;
+  location_dest.pResource = resource;
+  location_dest.Type = D3D12_TEXTURE_COPY_TYPE_SUBRESOURCE_INDEX;
+  for (uint32_t level = 0; level < mip_count; ++level) {
+    location_source.PlacedFootprint = footprints[level];
+    location_dest.SubresourceIndex = level;
+    command_list.D3DCopyTextureRegion(&location_dest, 0, 0, 0, &location_source, nullptr);
+  }
+  return true;
 }
 
 bool D3D12TextureCache::LoadTextureDataFromResidentMemoryImpl(Texture& texture, bool load_base,
                                                               bool load_mips) {
   D3D12Texture& d3d12_texture = static_cast<D3D12Texture&>(texture);
   TextureKey texture_key = d3d12_texture.key();
+  if (d3d12_texture.replacement()) {
+    return UploadReplacementTexture(d3d12_texture);
+  }
 
   DeferredCommandList& command_list = command_processor_.GetDeferredCommandList();
   ID3D12Device* device = command_processor_.GetD3D12Provider().GetDevice();
@@ -2075,6 +2204,10 @@ uint32_t D3D12TextureCache::FindOrCreateTextureDescriptor(D3D12Texture& texture,
   }
 
   uint32_t mip_levels = texture_key.mip_max_level + 1;
+  if (texture.replacement()) {
+    desc.Format = DXGI_FORMAT_R8G8B8A8_UNORM;
+    mip_levels = texture.replacement_mip_count();
+  }
   ID3D12Resource* resource_for_view = texture.resource();
   switch (dimension) {
     case xenos::DataDimension::k3D:
diff --git a/src/graphics/flags.cpp b/src/graphics/flags.cpp
index e2983a4..268a851 100644
--- a/src/graphics/flags.cpp
+++ b/src/graphics/flags.cpp
@@ -15,6 +15,16 @@
 
 REXCVAR_DEFINE_BOOL(gpu_allow_invalid_fetch_constants, false, "GPU",
                     "Allow invalid fetch constants");
+// DP1 (2026-09-06): guest texture dump / host texture replacement.
+REXCVAR_DEFINE_BOOL(texture_dump, false, "GPU",
+                    "Write every 2D texture the game loads as PNG into <texture_path>/dump");
+REXCVAR_DEFINE_BOOL(texture_replacement, true, "GPU",
+                    "Replace guest textures with <texture_path>/<hash>.png when present");
+REXCVAR_DEFINE_STRING(texture_trace_hash, "", "GPU",
+                      "Diagnostics: 16-hex texture hash; every draw binding it logs the blend / alpha "
+                      "test / colour mask registers once per distinct state");
+REXCVAR_DEFINE_STRING(texture_path, "", "GPU",
+                      "Texture dump/replacement folder (empty = \"textures\" next to the executable)");
 REXCVAR_DEFINE_BOOL(native_2x_msaa, true, "GPU", "Enable native 2x MSAA");
 REXCVAR_DEFINE_BOOL(depth_float24_round, false, "GPU", "Round float24 depth values");
 REXCVAR_DEFINE_BOOL(depth_float24_convert_in_pixel_shader, false, "GPU",
diff --git a/src/graphics/pipeline/render_target/cache.cpp b/src/graphics/pipeline/render_target/cache.cpp
index 953c5fd..82740ec 100644
--- a/src/graphics/pipeline/render_target/cache.cpp
+++ b/src/graphics/pipeline/render_target/cache.cpp
@@ -341,6 +341,7 @@ void RenderTargetCache::InitializeCommon() {
 }
 
 void RenderTargetCache::DestroyAllRenderTargets(bool shutting_down) {
+  std::memset(interlock_tile_is_7e3_, 0, sizeof(interlock_tile_is_7e3_));
   ownership_ranges_.clear();
   if (!shutting_down) {
     ownership_ranges_.emplace(std::piecewise_construct, std::forward_as_tuple(uint32_t(0)),
@@ -457,6 +458,9 @@ bool RenderTargetCache::Update(bool is_rasterization_done,
   // depth_and_color_rts_used_bits -> EDRAM base.
   uint32_t edram_bases[1 + xenos::kMaxColorRenderTargets];
   uint32_t resource_formats[1 + xenos::kMaxColorRenderTargets];
+  // DP1 fix (pixel shader interlock): the real guest color formats, as
+  // resource_formats only carries the pixel size on the interlock path.
+  xenos::ColorRenderTargetFormat guest_color_formats[1 + xenos::kMaxColorRenderTargets] = {};
   uint32_t rts_are_64bpp = 0;
   if (is_rasterization_done) {
     if (normalized_depth_control.z_enable || normalized_depth_control.stencil_enable) {
@@ -477,6 +481,7 @@ bool RenderTargetCache::Update(bool is_rasterization_done,
       edram_bases[rt_bit_index] = color_info.color_base;
       xenos::ColorRenderTargetFormat color_format =
           regs.Get<reg::RB_COLOR_INFO>(reg::RB_COLOR_INFO::rt_register_indices[i]).color_format;
+      guest_color_formats[rt_bit_index] = color_format;
       bool is_64bpp = xenos::IsColorRenderTargetFormat64bpp(color_format);
       if (is_64bpp) {
         rts_are_64bpp |= uint32_t(1) << rt_bit_index;
@@ -676,6 +681,17 @@ bool RenderTargetCache::Update(bool is_rasterization_done,
     if (interlock_barrier_needed) {
       RequestPixelShaderInterlockBarrier();
     }
+    // DP1 fix: convert tiles still holding 7e3 color that are now bound as
+    // 8888 color, and record what this draw is going to write. Done after the
+    // barrier request (the conversion itself commits all preceding interlocked
+    // writes anyway) and before ChangeOwnership, since a full barrier placed by
+    // the implementation resets the ownership map.
+    for (uint32_t i = 0; i < edram_bases_sorted_count; ++i) {
+      const std::pair<uint32_t, uint32_t>& rt_base_index = edram_bases_sorted[i];
+      uint32_t rt_bit_index = rt_base_index.second;
+      PixelShaderInterlockTrackTiles(rt_base_index.first, rt_lengths_tiles[i], rt_bit_index != 0,
+                                     guest_color_formats[rt_bit_index], true);
+    }
   }
 
   // From now on ownership transfers should succeed for simplicity and
@@ -1144,6 +1160,50 @@ void RenderTargetCache::PixelShaderInterlockFullEdramBarrierPlaced() {
   ownership_ranges_.emplace(0, empty_range);
 }
 
+void RenderTargetCache::PixelShaderInterlockTrackTiles(uint32_t start_tiles, uint32_t length_tiles,
+                                                       bool is_color,
+                                                       xenos::ColorRenderTargetFormat color_format,
+                                                       bool convert_if_7e3_to_8888) {
+  assert_true(GetPath() == Path::kPixelShaderInterlock);
+  if (!length_tiles) {
+    return;
+  }
+  length_tiles = std::min(length_tiles, xenos::kEdramTileCount);
+  start_tiles &= xenos::kEdramTileCount - 1;
+  bool is_7e3 =
+      is_color && (color_format == xenos::ColorRenderTargetFormat::k_2_10_10_10_FLOAT ||
+                   color_format == xenos::ColorRenderTargetFormat::k_2_10_10_10_FLOAT_AS_16_16_16_16);
+  bool is_8888 = is_color && (color_format == xenos::ColorRenderTargetFormat::k_8_8_8_8 ||
+                              color_format == xenos::ColorRenderTargetFormat::k_8_8_8_8_GAMMA);
+  // The range may wrap around the end of the EDRAM - handle it as up to two
+  // non-wrapping segments.
+  auto track_segment = [&](uint32_t segment_start, uint32_t segment_end) {
+    if (is_8888 && convert_if_7e3_to_8888) {
+      uint32_t run_start = segment_start;
+      bool in_run = false;
+      for (uint32_t tile = segment_start; tile <= segment_end; ++tile) {
+        bool tile_is_7e3 = tile < segment_end && interlock_tile_is_7e3_[tile] != 0;
+        if (tile_is_7e3) {
+          if (!in_run) {
+            in_run = true;
+            run_start = tile;
+          }
+        } else if (in_run) {
+          in_run = false;
+          PixelShaderInterlockReinterpret7e3To8888(run_start, tile - run_start);
+        }
+      }
+    }
+    std::memset(interlock_tile_is_7e3_ + segment_start, is_7e3 ? 1 : 0,
+                segment_end - segment_start);
+  };
+  uint32_t end_tiles = start_tiles + length_tiles;
+  track_segment(start_tiles, std::min(end_tiles, xenos::kEdramTileCount));
+  if (end_tiles > xenos::kEdramTileCount) {
+    track_segment(0, end_tiles - xenos::kEdramTileCount);
+  }
+}
+
 RenderTargetCache::RenderTarget* RenderTargetCache::GetOrCreateRenderTarget(RenderTargetKey key) {
   assert_true(GetPath() == Path::kHostRenderTargets);
   auto it_rt = render_targets_.find(key);
diff --git a/src/graphics/pipeline/shader/dxbc_translator_fetch.cpp b/src/graphics/pipeline/shader/dxbc_translator_fetch.cpp
index 31de175..6775922 100644
--- a/src/graphics/pipeline/shader/dxbc_translator_fetch.cpp
+++ b/src/graphics/pipeline/shader/dxbc_translator_fetch.cpp
@@ -10,6 +10,9 @@
  */
 
 #include <algorithm>
+#include <cstdlib>
+#include <string>
+#include <vector>
 #include <cmath>
 #include <memory>
 #include <sstream>
@@ -26,6 +29,52 @@
 
 REXCVAR_DEFINE_BOOL(draw_resolution_scaled_texture_offsets, true, "GPU/Shader",
                     "Scale texture offsets with draw resolution");
+// DP1 2026-09-06: at draw resolution scale > 1, screen-space post-process
+// passes (bloom / DoF blurs) keep their tap spacing in guest texels, so every
+// bilinear tap lands on a single host texel and the texels in between are
+// skipped - a regular grid appears on hair, foliage and glow (Deadly
+// Premonition). Shifting the coordinates of resolution-scaled (resolved)
+// textures by half a host texel turns each tap into an exact 2x2 host-texel
+// box average, i.e. the pass sees the same image it saw at 1x.
+REXCVAR_DEFINE_BOOL(gpu_resolved_texture_box_filter, false, "GPU/Shader",
+                    "Sample resolution-scaled render-target textures with a 2x2 host-texel box "
+                    "(half-texel shift) so guest-texel-spaced post-process taps do not alias")
+    .lifecycle(rex::cvar::Lifecycle::kRequiresRestart);
+// Applying the shift to every resolved-texture fetch also turns the final 1:1
+// scene copy into a 2x2 average (the whole frame drops to 1x), so it is
+// limited to the listed pixel shaders (ucode hashes, hex, comma-separated).
+// Default (Deadly Premonition): 17-tap separable blur 2124BC7D, 2x2 box
+// downsample 46331D61, 4-tap shadow PCF FA55491D. NOT the 3-tap full-screen
+// passes (FF5A2E8B chromatic split, 1530E2EB) - those run 1:1 and would soften
+// the whole frame.
+REXCVAR_DEFINE_STRING(gpu_resolved_texture_box_filter_shaders,
+                      "2124BC7DB86884DF,46331D613EE9AA78,FA55491D7925E74A,1530E2EB5CFF039B,FF5A2E8BBD6623F6",
+                      "GPU/Shader",
+                      "Pixel shader ucode hashes (hex, comma-separated) that get the resolved "
+                      "texture 2x2 box shift")
+    .lifecycle(rex::cvar::Lifecycle::kRequiresRestart);
+
+namespace {
+bool IsBoxFilterShader(uint64_t ucode_hash) {
+  static const std::vector<uint64_t> hashes = [] {
+    std::vector<uint64_t> result;
+    const std::string& list = REXCVAR_GET(gpu_resolved_texture_box_filter_shaders);
+    size_t pos = 0;
+    while (pos < list.size()) {
+      size_t end = list.find_first_of(", ;", pos);
+      if (end == std::string::npos) {
+        end = list.size();
+      }
+      if (end > pos) {
+        result.push_back(std::strtoull(list.substr(pos, end - pos).c_str(), nullptr, 16));
+      }
+      pos = end + 1;
+    }
+    return result;
+  }();
+  return std::find(hashes.begin(), hashes.end(), ucode_hash) != hashes.end();
+}
+}  // namespace
 
 namespace rex::graphics {
 using namespace ucode;
@@ -834,6 +883,18 @@ void DxbcShaderTranslator::ProcessTextureFetchInstruction(
     // the texture is 3D unconditionally.
     size_needed_components |= 0b1000;
   }
+  // DP1: 2x2 host-texel box shift for resolution-scaled textures (see the
+  // gpu_resolved_texture_box_filter cvar) - needs the texture size.
+  const bool resolved_box_shift =
+      REXCVAR_GET(gpu_resolved_texture_box_filter) && is_pixel_shader() &&
+      IsBoxFilterShader(current_shader().ucode_data_hash()) &&
+      instr.opcode == FetchOpcode::kTextureFetch &&
+      instr.dimension == xenos::FetchOpDimension::k2D &&
+      !instr.attributes.unnormalized_coordinates &&
+      (draw_resolution_scale_x_ > 1 || draw_resolution_scale_y_ > 1);
+  if (resolved_box_shift) {
+    size_needed_components |= 0b0011;
+  }
   uint32_t size_and_is_3d_temp = size_needed_components ? PushSystemTemp() : UINT32_MAX;
   if (size_needed_components) {
     switch (instr.dimension) {
@@ -1145,6 +1206,25 @@ void DxbcShaderTranslator::ProcessTextureFetchInstruction(
         }
       }
     }
+    if (resolved_box_shift) {
+      // DP1: if this fetch slot is a resolution-scaled texture, shift the
+      // normalized XY by half a HOST texel (= 0.5 / (guest size * scale)).
+      uint32_t shift_temp = PushSystemTemp();
+      a_.OpAnd(dxbc::Dest::R(shift_temp, 0b1000),
+               LoadSystemConstant(SystemConstants::Index::kTexturesResolutionScaled,
+                                  offsetof(SystemConstants, textures_resolution_scaled),
+                                  dxbc::Src::kXXXX),
+               dxbc::Src::LU(uint32_t(1) << tfetch_index));
+      a_.OpIf(true, dxbc::Src::R(shift_temp, dxbc::Src::kWWWW));
+      a_.OpDiv(dxbc::Dest::R(shift_temp, 0b0011),
+               dxbc::Src::LF(0.5f / float(draw_resolution_scale_x_),
+                             0.5f / float(draw_resolution_scale_y_), 0.0f, 0.0f),
+               dxbc::Src::R(size_and_is_3d_temp));
+      a_.OpAdd(dxbc::Dest::R(coord_and_sampler_temp, 0b0011),
+               dxbc::Src::R(coord_and_sampler_temp), dxbc::Src::R(shift_temp));
+      a_.OpEndIf();
+      PopSystemTemp();
+    }
     switch (instr.dimension) {
       case xenos::FetchOpDimension::k1D:
         // Pad to 2D array coordinates.
diff --git a/src/graphics/pipeline/texture/cache.cpp b/src/graphics/pipeline/texture/cache.cpp
index 76f5600..f27602c 100644
--- a/src/graphics/pipeline/texture/cache.cpp
+++ b/src/graphics/pipeline/texture/cache.cpp
@@ -10,7 +10,12 @@
  */
 
 #include <algorithm>
+#include <array>
+#include <mutex>
+#include <set>
+#include <vector>
 #include <cstdint>
+#include <cstdlib>
 #include <utility>
 
 #include <rex/assert.h>
@@ -20,6 +25,7 @@
 #include <rex/graphics/flags.h>
 #include <rex/graphics/pipeline/texture/cache.h>
 #include <rex/graphics/pipeline/texture/info.h>
+#include <rex/graphics/pipeline/texture/replacement.h>
 #include <rex/graphics/pipeline/texture/util.h>
 #include <rex/graphics/register_file.h>
 #include <rex/graphics/xenos.h>
@@ -246,6 +252,8 @@ bool TextureCache::GetConfigDrawResolutionScale(uint32_t& x_out, uint32_t& y_out
   uint32_t clamped_y = std::min(kMaxDrawResolutionScaleAlongAxis, config_y);
   x_out = clamped_x;
   y_out = clamped_y;
+  REXGPU_INFO("Draw resolution scale: {}x{} (resolution_scale cvar {}, config {}x{})", clamped_x,
+              clamped_y, REXCVAR_GET(resolution_scale), config_x, config_y);
   return clamped_x == config_x && clamped_y == config_y;
 }
 
@@ -591,6 +599,41 @@ void TextureCache::RequestTextures(uint32_t used_texture_mask) {
   if (bindings_changed) {
     UpdateTextureBindingsImpl(bindings_changed);
   }
+  // DP1 diagnostics: texture_trace_hash = <16 hex> logs the raster/blend state
+  // of every draw that binds that texture (once per distinct state).
+  {
+    static uint64_t trace_hash = 0;
+    static bool trace_parsed = false;
+    if (!trace_parsed) {
+      trace_parsed = true;
+      const std::string h = REXCVAR_GET(texture_trace_hash);
+      if (h.size() >= 16) trace_hash = std::strtoull(h.substr(0, 16).c_str(), nullptr, 16);
+    }
+    if (trace_hash) {
+      for (uint32_t i = 0; i < 32; ++i) {
+        if (!(used_texture_mask & (uint32_t(1) << i))) continue;
+        const TextureBinding& b = texture_bindings_[i];
+        if (!b.texture || b.texture->data_hash() != trace_hash) continue;
+        const uint32_t blend0 = regs[XE_GPU_REG_RB_BLENDCONTROL0];
+        const uint32_t colorcontrol = regs[XE_GPU_REG_RB_COLORCONTROL];
+        const uint32_t colormask = regs[XE_GPU_REG_RB_COLOR_MASK];
+        const uint32_t depthcontrol = regs[XE_GPU_REG_RB_DEPTHCONTROL];
+        const uint32_t alpharef = regs[XE_GPU_REG_RB_ALPHA_REF];
+        const uint32_t colorinfo = regs[XE_GPU_REG_RB_COLOR_INFO];
+        const uint32_t modecontrol = regs[XE_GPU_REG_RB_MODECONTROL];
+        static std::mutex seen_mutex;
+        static std::set<std::array<uint32_t, 8>> seen;
+        std::array<uint32_t, 8> k = {i, blend0, colorcontrol, colormask, depthcontrol, alpharef, colorinfo, modecontrol};
+        std::lock_guard<std::mutex> lock(seen_mutex);
+        if (seen.insert(k).second) {
+          REXGPU_INFO("Texture trace {:016X}: fetch#{} blend0={:08X} colorcontrol={:08X} colormask={:08X} "
+                      "depthcontrol={:08X} alpharef={:08X} colorinfo={:08X} modecontrol={:08X}",
+                      trace_hash, i, blend0, colorcontrol, colormask, depthcontrol, alpharef, colorinfo,
+                      modecontrol);
+        }
+      }
+    }
+  }
 }
 
 const char* TextureCache::TextureKey::GetLogDimensionName(xenos::DataDimension dimension) {
@@ -867,6 +910,7 @@ TextureCache::Texture* TextureCache::FindOrCreateTexture(TextureKey key) {
   // Create the texture and add it to the map.
   Texture* texture;
   {
+    creating_data_hash_ = HashGuestTexture(key, creating_view_);
     std::unique_ptr<Texture> new_texture = CreateTexture(key);
     if (!new_texture) {
       key.LogAction("Failed to create");
@@ -874,12 +918,46 @@ TextureCache::Texture* TextureCache::FindOrCreateTexture(TextureKey key) {
     }
     assert_true(new_texture->key() == key);
     texture = textures_.emplace(key, std::move(new_texture)).first->second.get();
+    texture->SetDataHash(creating_data_hash_);
+    creating_data_hash_ = 0;
   }
   COUNT_profile_set("gpu/texture_cache/textures", textures_.size());
   texture->LogAction("Created");
   return texture;
 }
 
+uint64_t TextureCache::HashGuestTexture(const TextureKey& key,
+                                        texture_replacement::GuestBaseView& view_out) const {
+  view_out = texture_replacement::GuestBaseView();
+  texture_replacement::Registry& registry = texture_replacement::Registry::Get();
+  if (!registry.active()) return 0;
+  if (!key.base_page || key.scaled_resolve || key.signed_separate ||
+      key.dimension != xenos::DataDimension::k2DOrStacked || key.GetDepthOrArraySize() != 1) {
+    return 0;
+  }
+  texture_util::TextureGuestLayout layout = key.GetGuestLayout();
+  // packed_level == 0 means the base itself lives inside the packed mip tail.
+  if (layout.packed_level == 0 || !layout.base.level_data_extent_bytes) return 0;
+  texture_replacement::GuestBaseView view;
+  view.data = shared_memory().guest_memory().TranslatePhysical<const uint8_t*>(key.base_page << 12);
+  view.size_bytes = layout.base.level_data_extent_bytes;
+  view.guest_address = key.base_page << 12;
+  view.width = key.GetWidth();
+  view.height = key.GetHeight();
+  view.format = key.format;
+  view.endian = key.endianness;
+  view.tiled = key.tiled != 0;
+  view.row_pitch_bytes = layout.base.row_pitch_bytes;
+  view.x_extent_blocks = layout.base.x_extent_blocks;
+  view.y_extent_blocks = layout.base.y_extent_blocks;
+  uint64_t hash = registry.Hash(view);
+  if (registry.dump_enabled()) {
+    registry.Dump(hash, view);
+  }
+  view_out = view;
+  return hash;
+}
+
 bool TextureCache::LoadTextureData(Texture& texture) {
   PendingTextureLoad pending_load;
   PendingSharedMemoryRange pending_ranges[2];
@@ -914,11 +992,28 @@ void TextureCache::BindingInfoFromFetchConstant(const xenos::xe_gpu_texture_fetc
       if (REXCVAR_GET(gpu_allow_invalid_fetch_constants)) {
         break;
       }
+      {
+        // DP1 (2026-09-06): Deadly Premonition binds the same three "invalid"
+        // fetch constants every frame, flooding the log at 3 lines per frame.
+        // Report each distinct constant once (small bounded set).
+        static std::mutex reported_mutex;
+        static std::vector<std::array<uint32_t, 6>> reported;
+        std::array<uint32_t, 6> words = {fetch.dword_0, fetch.dword_1, fetch.dword_2,
+                                         fetch.dword_3, fetch.dword_4, fetch.dword_5};
+        std::lock_guard<std::mutex> lock(reported_mutex);
+        if (std::find(reported.begin(), reported.end(), words) != reported.end()) {
+          return;
+        }
+        if (reported.size() < 64) {
+          reported.push_back(words);
+        }
+      }
       REXGPU_WARN(
           "Texture fetch constant ({:08X} {:08X} {:08X} {:08X} {:08X} {:08X}) "
           "has \"invalid\" type! This is incorrect behavior, but you can try "
           "bypassing this by launching Xenia with "
-          "--gpu_allow_invalid_fetch_constants=true.",
+          "--gpu_allow_invalid_fetch_constants=true. (Reported once per distinct "
+          "constant.)",
           fetch.dword_0, fetch.dword_1, fetch.dword_2, fetch.dword_3, fetch.dword_4, fetch.dword_5);
       return;
     default:
diff --git a/src/input/mnk/mnk_input_driver.cpp b/src/input/mnk/mnk_input_driver.cpp
index 3f9422d..6716044 100644
--- a/src/input/mnk/mnk_input_driver.cpp
+++ b/src/input/mnk/mnk_input_driver.cpp
@@ -1,3 +1,4 @@
+#include <chrono>
 /**
  * @file        input/mnk/mnk_input_driver.cpp
  * @brief       Keyboard/mouse input driver implementation.
@@ -29,6 +30,50 @@ REXCVAR_DEFINE_BOOL(mnk_mouse, false, "Input",
                     "from the keybind_rstick_* keys only");
 REXCVAR_DEFINE_DOUBLE(mnk_sensitivity, 1.0, "Input", "Mouse sensitivity for right stick")
     .range(0.01, 10.0);
+// DP1 2026-09-06 (ported from the Downpour SDK fork, mnk_input_driver.cpp
+// "raw path"): mouse deltas map to right-stick deflection DIRECTLY, with a
+// floor above the game's inner right-stick deadzone. The stock mapping
+// (delta * sensitivity * 200) produced 200-600 for ordinary 1-3 px moves,
+// which the game discards below its ~8689 deadzone - that read as "slow,
+// dead mouse that only reacts to flicks" in Deadly Premonition (and Downpour).
+REXCVAR_DEFINE_DOUBLE(mnk_stick_scale, 300.0, "Input",
+                      "Right-stick units per mouse pixel (per poll) on top of the deadzone floor; "
+                      "multiplied by mnk_sensitivity")
+    .range(10.0, 5000.0);
+REXCVAR_DEFINE_INT32(mnk_deadzone_floor, 8689, "Input",
+                     "Minimum right-stick deflection for any nonzero mouse motion (XInput "
+                     "canonical inner deadzone is 8689; 0 = no floor)")
+    .range(0, 32767);
+REXCVAR_DEFINE_BOOL(mnk_auto_shake, false, "Input/MnK",
+                    "Hold the left and right movement keys together to shake the left stick "
+                    "automatically (stick-shake QTEs such as \"Get it off!\")")
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_DOUBLE(mnk_key_stick_ramp_ms, 60.0, "Input/MnK",
+                      "Keyboard left stick travel time from centre to full deflection (and back) "
+                      "in ms, like the PC port: tapping A/D alternately sweeps through the centre "
+                      "so stick-shake QTEs count every tap. 0 = instant (1.0 behaviour)")
+    .range(0.0, 300.0)
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_DOUBLE(mnk_tap_shake_window_ms, 250.0, "Input/MnK",
+                      "Tap-to-shake: two alternating left/right movement key presses closer "
+                      "than this (ms) start a full-amplitude left-stick shake (stick-shake "
+                      "QTEs such as \"Get it off!\", tap A > D > A > D like the PC port). 0 = off")
+    .range(0.0, 1000.0)
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_DOUBLE(mnk_tap_shake_hold_ms, 300.0, "Input/MnK",
+                      "Tap-to-shake: keep shaking this long (ms) after the last tap")
+    .range(0.0, 2000.0)
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_DOUBLE(mnk_wheel_rstick_ms, 120.0, "Input/MnK",
+                      "Outside gameplay cameras (menus, the map) each mouse wheel notch also holds "
+                      "the right stick up/down for this long (ms) - map zoom with the wheel. 0 = off")
+    .range(0.0, 1000.0)
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_DOUBLE(mnk_auto_shake_hz, 12.0, "Input/MnK",
+                      "Auto-shake oscillation rate in full left-right cycles per second")
+    .range(2.0, 30.0)
+    .lifecycle(rex::cvar::Lifecycle::kHotReload);
+REXCVAR_DEFINE_BOOL(mnk_invert_y, false, "Input", "Invert mouse Y for the right stick");
 
 REXCVAR_DEFINE_STRING(keybind_a, "Semicolon,Space", "Input/Keybinds/Controller", "A button");
 REXCVAR_DEFINE_STRING(keybind_b, "Quote,Backspace", "Input/Keybinds/Controller", "B button");
@@ -71,6 +116,13 @@ constexpr rex::input::DeviceId kMnkDevice = static_cast<rex::input::DeviceId>(0x
 // it. That is what lets the D-pad share the arrow keys. A consequence is
 // that binding a bare modifier name ("Shift") can never fire, since holding it
 // makes the live mask non-zero while the bind wants zero.
+//
+// DP1 port 2026-09-05 (from dpour fork ce7d25d, mnk_input_driver.cpp): inside
+// one alternative, '+' may also join several non-modifier keys into an AND
+// combo, "Space+LMB" or "Shift+Space+LMB". Every listed key has to be down and
+// the modifier mask still has to match exactly, so all pre-existing bind
+// strings keep their meaning. "WheelUp" / "WheelDown" are ordinary key names
+// here; the driver pulses them for one poll per scroll detent.
 constexpr uint8_t kModShift = 1u << 0;
 constexpr uint8_t kModCtrl = 1u << 1;
 constexpr uint8_t kModAlt = 1u << 2;
@@ -86,30 +138,18 @@ uint8_t LiveModifiers(const bool (&key_down)[256]) {
   return mods;
 }
 
-// Strips leading modifier prefixes off 'token', advancing it to the bare key
-// name and returning the mask they require.
-uint8_t TakeModifiers(std::string_view& token) {
-  uint8_t mods = 0;
-  for (;;) {
-    size_t plus = token.find('+');
-    if (plus == std::string_view::npos || plus == 0) {
-      break;
-    }
-    std::string_view head = token.substr(0, plus);
-    uint8_t bit = 0;
-    if (head == "Shift") {
-      bit = kModShift;
-    } else if (head == "Ctrl" || head == "Control") {
-      bit = kModCtrl;
-    } else if (head == "Alt") {
-      bit = kModAlt;
-    } else {
-      break;
-    }
-    mods |= bit;
-    token.remove_prefix(plus + 1);
+// Modifier mask bit for a modifier token name, or 0 for anything else.
+uint8_t ModifierBit(std::string_view token) {
+  if (token == "Shift") {
+    return kModShift;
   }
-  return mods;
+  if (token == "Ctrl" || token == "Control") {
+    return kModCtrl;
+  }
+  if (token == "Alt") {
+    return kModAlt;
+  }
+  return 0;
 }
 
 std::string_view TrimSpaces(std::string_view s) {
@@ -122,17 +162,99 @@ std::string_view TrimSpaces(std::string_view s) {
   return s;
 }
 
-bool TokenPressed(const bool (&key_down)[256], std::string_view token, uint8_t live_mods) {
-  uint8_t want = TakeModifiers(token);
-  if (want != live_mods) {
+// DP1 v1.1 (DPRecomp #11 "holding Shift freezes York"): bind evaluation.
+// One comma-separated alternative = modifier names + key names joined by '+'.
+// Rules:
+//   * every listed key must be down and every listed modifier must be held;
+//     OTHER modifiers being held no longer silence a bind ("W" keeps working
+//     while Shift = X button is held, so run = Shift + WASD works);
+//   * a bare modifier group ("Shift") fires while that modifier is held;
+//   * shadowing keeps the D-pad on the arrow keys: a group is silent while a
+//     MORE specific configured group (same keys, strictly more modifiers) is
+//     satisfied, so "Left" stays quiet while "Shift+Left" fires.
+struct ParsedGroup {
+  uint8_t mods = 0;
+  std::vector<uint16_t> keys;  // sorted
+  bool valid = false;
+};
+
+ParsedGroup ParseGroup(std::string_view group) {
+  ParsedGroup out;
+  bool bad = false;
+  while (!group.empty()) {
+    size_t plus = group.find('+');
+    std::string_view token = TrimSpaces(group.substr(0, plus));
+    if (plus == std::string_view::npos) {
+      group = std::string_view();
+    } else {
+      group.remove_prefix(plus + 1);
+    }
+    if (token.empty()) {
+      continue;
+    }
+    uint8_t bit = ModifierBit(token);
+    if (bit) {
+      out.mods |= bit;
+      continue;
+    }
+    rex::ui::VirtualKey vk = rex::ui::ParseVirtualKey(token);
+    uint16_t idx = static_cast<uint16_t>(vk);
+    if (vk == rex::ui::VirtualKey::kNone || idx >= 256) {
+      bad = true;
+      break;
+    }
+    out.keys.push_back(idx);
+  }
+  std::sort(out.keys.begin(), out.keys.end());
+  out.valid = !bad && (out.mods != 0 || !out.keys.empty());
+  return out;
+}
+
+bool GroupSatisfied(const ParsedGroup& g, const bool (&key_down)[256], uint8_t live_mods) {
+  if (!g.valid || (live_mods & g.mods) != g.mods) {
     return false;
   }
-  rex::ui::VirtualKey vk = rex::ui::ParseVirtualKey(token);
-  if (vk == rex::ui::VirtualKey::kNone) {
+  for (uint16_t k : g.keys) {
+    if (!key_down[k]) {
+      return false;
+    }
+  }
+  return true;
+}
+
+// All configured groups of the current poll (for shadowing); filled at the
+// top of GetState under state_mutex_.
+std::vector<ParsedGroup> g_all_groups;
+
+void ForEachGroup(const std::string& cvar_val, const std::function<void(std::string_view)>& fn) {
+  std::string_view rest(cvar_val);
+  while (!rest.empty()) {
+    size_t comma = rest.find(',');
+    std::string_view token = TrimSpaces(rest.substr(0, comma));
+    if (comma == std::string_view::npos) {
+      rest = std::string_view();
+    } else {
+      rest.remove_prefix(comma + 1);
+    }
+    if (!token.empty()) {
+      fn(token);
+    }
+  }
+}
+
+bool GroupPressed(const bool (&key_down)[256], std::string_view group, uint8_t live_mods) {
+  ParsedGroup g = ParseGroup(group);
+  if (!GroupSatisfied(g, key_down, live_mods)) {
     return false;
   }
-  uint16_t idx = static_cast<uint16_t>(vk);
-  return idx < 256 && key_down[idx];
+  // Shadowed by a more specific configured group?
+  for (const ParsedGroup& other : g_all_groups) {
+    if (other.keys == g.keys && other.mods != g.mods && (other.mods & g.mods) == g.mods &&
+        GroupSatisfied(other, key_down, live_mods)) {
+      return false;
+    }
+  }
+  return true;
 }
 
 std::atomic<bool> mouse_look_active{true};
@@ -147,6 +269,56 @@ bool IsMouseLookActive() {
   return mouse_look_active.load(std::memory_order_relaxed);
 }
 
+// DP1: camera hook state + raw delta accumulator (see the header).
+std::atomic<bool> mouse_camera_hook_active{false};
+std::atomic<float> mouse_camera_dx{0.0f};
+std::atomic<float> mouse_camera_dy{0.0f};
+
+void SetMouseCameraHookActive(bool active) {
+  mouse_camera_hook_active.store(active, std::memory_order_relaxed);
+}
+
+bool IsMouseCameraHookActive() {
+  return mouse_camera_hook_active.load(std::memory_order_relaxed);
+}
+
+std::atomic<int64_t> mouse_camera_hook_frame_ns{0};
+
+void TakeMouseCameraDelta(float& dx_out, float& dy_out) {
+  dx_out = mouse_camera_dx.exchange(0.0f, std::memory_order_relaxed);
+  dy_out = mouse_camera_dy.exchange(0.0f, std::memory_order_relaxed);
+  const auto now = std::chrono::steady_clock::now().time_since_epoch();
+  mouse_camera_hook_frame_ns.store(
+      std::chrono::duration_cast<std::chrono::nanoseconds>(now).count(), std::memory_order_relaxed);
+}
+
+bool IsMouseCameraHookRecent() {
+  const int64_t last = mouse_camera_hook_frame_ns.load(std::memory_order_relaxed);
+  if (last == 0) return false;
+  const auto now = std::chrono::steady_clock::now().time_since_epoch();
+  const int64_t now_ns = std::chrono::duration_cast<std::chrono::nanoseconds>(now).count();
+  return now_ns - last < 200'000'000;
+}
+
+// DP1 v1.1: "aim camera ran this frame" stamp (steady clock, ns).
+std::atomic<int64_t> mouse_aim_frame_ns{0};
+
+void NoteMouseAimFrame() {
+  const auto now = std::chrono::steady_clock::now().time_since_epoch();
+  mouse_aim_frame_ns.store(std::chrono::duration_cast<std::chrono::nanoseconds>(now).count(),
+                           std::memory_order_relaxed);
+}
+
+bool IsMouseAimStickActive() {
+  const int64_t last = mouse_aim_frame_ns.load(std::memory_order_relaxed);
+  if (last == 0) {
+    return false;
+  }
+  const auto now = std::chrono::steady_clock::now().time_since_epoch();
+  const int64_t now_ns = std::chrono::duration_cast<std::chrono::nanoseconds>(now).count();
+  return (now_ns - last) < 150 * 1000000LL;
+}
+
 using rex::ui::VirtualKey;
 
 MnkInputDriver::MnkInputDriver(rex::ui::Window* window, size_t window_z_order)
@@ -212,7 +384,8 @@ static bool IsBindPressed(const bool (&key_down)[256], const std::string& cvar_v
       rest.remove_prefix(comma + 1);
     }
     token = TrimSpaces(token);
-    if (!token.empty() && TokenPressed(key_down, token, live_mods)) {
+    // DP1 port 2026-09-05: each alternative may be an AND combo now.
+    if (!token.empty() && GroupPressed(key_down, token, live_mods)) {
       return true;
     }
   }
@@ -222,8 +395,18 @@ static bool IsBindPressed(const bool (&key_down)[256], const std::string& cvar_v
 void MnkInputDriver::EnumerateDevices(std::vector<DeviceInfo>& out) {
   // Disabled means no device at all, so it never occupies a guest user slot.
   if (!IsEnabled()) {
+    static bool warned_disabled = false;
+    if (!warned_disabled) {
+      warned_disabled = true;
+      REXLOG_INFO("MnK: mnk_mode is false - keyboard/mouse device NOT enumerated");
+    }
     return;
   }
+  static bool logged_enabled = false;
+  if (!logged_enabled) {
+    logged_enabled = true;
+    REXLOG_INFO("MnK: keyboard/mouse device enumerated (mnk_mode=true, mnk_mouse={})", REXCVAR_GET(mnk_mouse));
+  }
   DeviceInfo info;
   info.id = kMnkDevice;
   info.name = "Keyboard and Mouse";
@@ -269,11 +452,49 @@ X_RESULT MnkInputDriver::GetDeviceState(DeviceId id, X_INPUT_STATE* out_state) {
       std::memset(out_state, 0, sizeof(*out_state));
       out_state->packet_number = packet_number_;
     }
+    // DP1 port 2026-09-05: drop scroll gathered while the device was not the
+    // active one, or it would replay as a burst of presses on reactivation.
+    std::lock_guard lock(state_mutex_);
+    wheel_accumulator_y_ = 0;
     return X_ERROR_SUCCESS;
   }
 
   std::lock_guard lock(state_mutex_);
 
+  // DP1 port 2026-09-05 (from dpour fork ce7d25d, mnk_input_driver.cpp):
+  // drain one mouse-wheel detent per poll and pulse the synthetic kWheelUp /
+  // kWheelDown keys for exactly this poll, so binds referencing "WheelUp" /
+  // "WheelDown" register as one button press per scroll detent.
+  constexpr int32_t kWheelDetent = int32_t(rex::ui::MouseEvent::kScrollPerDetent);
+  bool wheel_up_pulse = false;
+  bool wheel_down_pulse = false;
+  if (wheel_accumulator_y_ >= kWheelDetent) {
+    wheel_accumulator_y_ -= kWheelDetent;
+    wheel_up_pulse = true;
+  } else if (wheel_accumulator_y_ <= -kWheelDetent) {
+    wheel_accumulator_y_ += kWheelDetent;
+    wheel_down_pulse = true;
+  }
+  key_down_[static_cast<uint16_t>(VirtualKey::kWheelUp)] = wheel_up_pulse;
+  key_down_[static_cast<uint16_t>(VirtualKey::kWheelDown)] = wheel_down_pulse;
+
+  // DP1 v1.1: snapshot every configured group for bind shadowing (see GroupPressed).
+  g_all_groups.clear();
+  for (const std::string* bind : {&REXCVAR_GET(keybind_a), &REXCVAR_GET(keybind_b), &REXCVAR_GET(keybind_x),
+                                  &REXCVAR_GET(keybind_y), &REXCVAR_GET(keybind_left_shoulder),
+                                  &REXCVAR_GET(keybind_right_shoulder), &REXCVAR_GET(keybind_lstick_press),
+                                  &REXCVAR_GET(keybind_rstick_press), &REXCVAR_GET(keybind_back),
+                                  &REXCVAR_GET(keybind_start), &REXCVAR_GET(keybind_guide),
+                                  &REXCVAR_GET(keybind_dpad_up), &REXCVAR_GET(keybind_dpad_down),
+                                  &REXCVAR_GET(keybind_dpad_left), &REXCVAR_GET(keybind_dpad_right),
+                                  &REXCVAR_GET(keybind_left_trigger), &REXCVAR_GET(keybind_right_trigger),
+                                  &REXCVAR_GET(keybind_lstick_left), &REXCVAR_GET(keybind_lstick_right),
+                                  &REXCVAR_GET(keybind_lstick_up), &REXCVAR_GET(keybind_lstick_down),
+                                  &REXCVAR_GET(keybind_rstick_left), &REXCVAR_GET(keybind_rstick_right),
+                                  &REXCVAR_GET(keybind_rstick_up), &REXCVAR_GET(keybind_rstick_down)}) {
+    ForEachGroup(*bind, [](std::string_view grp) { g_all_groups.push_back(ParseGroup(grp)); });
+  }
+
   uint16_t buttons = 0;
   if (IsBindPressed(key_down_, REXCVAR_GET(keybind_a)))
     buttons |= X_INPUT_GAMEPAD_A;
@@ -311,14 +532,82 @@ X_RESULT MnkInputDriver::GetDeviceState(DeviceId id, X_INPUT_STATE* out_state) {
 
   int32_t lx = 0;
   int32_t ly = 0;
-  if (IsBindPressed(key_down_, REXCVAR_GET(keybind_lstick_left)))
-    lx -= INT16_MAX;
-  if (IsBindPressed(key_down_, REXCVAR_GET(keybind_lstick_right)))
-    lx += INT16_MAX;
+  const bool lstick_left_down = IsBindPressed(key_down_, REXCVAR_GET(keybind_lstick_left));
+  const bool lstick_right_down = IsBindPressed(key_down_, REXCVAR_GET(keybind_lstick_right));
+  // DP1 v1.1.1: tap-to-shake. Rapid alternating taps of the left/right
+  // movement keys (A > D > A > D, PC-port style) drive a full-amplitude
+  // oscillation for mnk_tap_shake_hold_ms after the last tap. The slew-rate
+  // ramp alone left short taps far from the extremes and "Get it off!" did
+  // not register. Press edges are taken from key_pressed_since_poll_ so a
+  // tap shorter than one poll still counts.
+  bool tap_shake = false;
+  {
+    static std::chrono::steady_clock::time_point last_edge{}, prev_edge{};
+    static int last_side = 0;
+    const auto now_tp = std::chrono::steady_clock::now();
+    const bool left_edge = IsBindPressed(key_pressed_since_poll_, REXCVAR_GET(keybind_lstick_left));
+    const bool right_edge = IsBindPressed(key_pressed_since_poll_, REXCVAR_GET(keybind_lstick_right));
+    const int side = left_edge ? -1 : (right_edge ? 1 : 0);
+    if (side != 0 && side != last_side) {
+      prev_edge = last_edge;
+      last_edge = now_tp;
+      last_side = side;
+    }
+    const double window_ms = REXCVAR_GET(mnk_tap_shake_window_ms);
+    if (window_ms > 0.0 && prev_edge.time_since_epoch().count() != 0) {
+      const double gap_ms = std::chrono::duration<double, std::milli>(last_edge - prev_edge).count();
+      const double since_ms = std::chrono::duration<double, std::milli>(now_tp - last_edge).count();
+      tap_shake = gap_ms <= window_ms && since_ms <= REXCVAR_GET(mnk_tap_shake_hold_ms);
+    }
+  }
+  std::memset(key_pressed_since_poll_, 0, sizeof(key_pressed_since_poll_));
+  if (tap_shake || (lstick_left_down && lstick_right_down && REXCVAR_GET(mnk_auto_shake))) {
+    // DP1 v1.1: "shake the left stick" QTEs (Get it off!) count stick sweeps;
+    // alternating A/D on a keyboard jumps between the extremes without a sweep
+    // and half the shakes are missed. Holding both keys makes the driver
+    // oscillate the stick left/right at mnk_auto_shake_hz, passing through
+    // the centre like a real stick.
+    const double hz = std::max(1.0, REXCVAR_GET(mnk_auto_shake_hz));
+    const double t = std::chrono::duration<double>(
+                         std::chrono::steady_clock::now().time_since_epoch())
+                         .count();
+    lx = static_cast<int32_t>(std::lround(std::sin(t * hz * 2.0 * 3.14159265358979) * INT16_MAX));
+  } else {
+    if (lstick_left_down) lx -= INT16_MAX;
+    if (lstick_right_down) lx += INT16_MAX;
+  }
   if (IsBindPressed(key_down_, REXCVAR_GET(keybind_lstick_up)))
     ly += INT16_MAX;
   if (IsBindPressed(key_down_, REXCVAR_GET(keybind_lstick_down)))
     ly -= INT16_MAX;
+  // DP1 v1.1: slew-rate limit the keyboard left stick (PC-port feel). The
+  // targets above are +-32767 / 0; the emitted value travels towards the
+  // target at full-scale / mnk_key_stick_ramp_ms, so A > D > A > D taps sweep
+  // through the centre instead of jumping between the extremes.
+  {
+    static double kb_lx = 0.0, kb_ly = 0.0;
+    static std::chrono::steady_clock::time_point kb_last{};
+    const auto now_tp = std::chrono::steady_clock::now();
+    const double ramp_ms = REXCVAR_GET(mnk_key_stick_ramp_ms);
+    if (ramp_ms <= 0.0 || kb_last.time_since_epoch().count() == 0) {
+      kb_lx = double(lx);
+      kb_ly = double(ly);
+    } else {
+      double dt_ms = std::chrono::duration<double, std::milli>(now_tp - kb_last).count();
+      dt_ms = std::clamp(dt_ms, 0.0, 100.0);
+      const double step = double(INT16_MAX) * dt_ms / ramp_ms;
+      auto approach = [step](double cur, double target) {
+        if (target > cur) return std::min(cur + step, target);
+        if (target < cur) return std::max(cur - step, target);
+        return cur;
+      };
+      kb_lx = tap_shake ? double(lx) : approach(kb_lx, double(lx));
+      kb_ly = approach(kb_ly, double(ly));
+    }
+    kb_last = now_tp;
+    lx = static_cast<int32_t>(std::lround(kb_lx));
+    ly = static_cast<int32_t>(std::lround(kb_ly));
+  }
 
   int32_t rx = 0;
   int32_t ry = 0;
@@ -330,12 +619,62 @@ X_RESULT MnkInputDriver::GetDeviceState(DeviceId id, X_INPUT_STATE* out_state) {
     ry += INT16_MAX;
   if (IsBindPressed(key_down_, REXCVAR_GET(keybind_rstick_down)))
     ry -= INT16_MAX;
+  // DP1 v1.1.1: mouse wheel -> right stick Y while no gameplay camera runs
+  // (the map zooms with the right stick). The wheel keeps its D-pad binding.
+  {
+    static std::chrono::steady_clock::time_point wheel_up_until{}, wheel_down_until{};
+    const double hold_ms = REXCVAR_GET(mnk_wheel_rstick_ms);
+    const auto now_tp = std::chrono::steady_clock::now();
+    if (wheel_up_pulse) wheel_up_until = now_tp + std::chrono::milliseconds(int64_t(hold_ms));
+    if (wheel_down_pulse) wheel_down_until = now_tp + std::chrono::milliseconds(int64_t(hold_ms));
+    const bool camera_live = IsMouseCameraHookActive() && IsMouseCameraHookRecent();
+    if (hold_ms > 0.0 && !camera_live) {
+      if (now_tp < wheel_up_until) ry += INT16_MAX;
+      if (now_tp < wheel_down_until) ry -= INT16_MAX;
+    }
+  }
 
-  if (REXCVAR_GET(mnk_mouse) && IsMouseLookActive()) {
-    double sensitivity = REXCVAR_GET(mnk_sensitivity);
-    constexpr double kBaseScale = 200.0;
-    rx += static_cast<int32_t>(double(mouse_dx_) * sensitivity * kBaseScale);
-    ry += static_cast<int32_t>(double(-mouse_dy_) * sensitivity * kBaseScale);
+  // DP1 v1.1: while the game's aim camera runs, the mouse becomes the LEFT
+  // stick (the aim camera's native input); the camera hook path is bypassed.
+  // v1.1 rev.2: the right trigger (aim) held is the primary signal - the game
+  // aims with the LEFT stick while RT is down, whatever camera routine runs;
+  // the aim-camera hook stamp stays as a secondary signal.
+  // DP1 v1.1.1: the camera hook only owns the mouse while a gameplay camera is
+  // actually running; in menus and the map the mouse is the right stick again.
+  const bool hook_live = IsMouseCameraHookActive() && IsMouseCameraHookRecent();
+  if (IsMouseCameraHookActive() && !hook_live) {
+    float drop_dx = 0.0f, drop_dy = 0.0f;
+    TakeMouseCameraDelta(drop_dx, drop_dy);  // keep the stamp fresh-free: no snap when the camera resumes
+    mouse_camera_hook_frame_ns.store(0, std::memory_order_relaxed);
+  }
+  const bool aim_stick = hook_live && (rt != 0 || IsMouseAimStickActive());
+  if (aim_stick) {
+    float drop_dx = 0.0f, drop_dy = 0.0f;
+    TakeMouseCameraDelta(drop_dx, drop_dy);  // never replay aim motion as a camera snap later
+  }
+  if (REXCVAR_GET(mnk_mouse) && IsMouseLookActive() && (!hook_live || aim_stick)) {
+    // DP1 (rev. 2, 2026-09-06): VECTOR mapping. The deadzone floor is added to
+    // the length of the mouse motion vector, not per axis - a per-axis floor
+    // turned every small move into a 45-degree diagonal ("crooked"). The
+    // direction of the mouse motion is preserved exactly; only the magnitude
+    // is remapped: stick length = floor + delta length * sensitivity * scale.
+    // Motion this poll = deflection this poll; no motion = instant zero.
+    const double dx = double(mouse_dx_);
+    // Stick Y is up-positive, screen Y is down-positive.
+    const double dy = REXCVAR_GET(mnk_invert_y) ? double(mouse_dy_) : -double(mouse_dy_);
+    const double len = std::sqrt(dx * dx + dy * dy);
+    if (len > 0.0) {
+      const double gain = REXCVAR_GET(mnk_sensitivity) * REXCVAR_GET(mnk_stick_scale);
+      const double floor = double(std::clamp(REXCVAR_GET(mnk_deadzone_floor), 0, 32767));
+      const double out_len = std::min(floor + len * gain, double(INT16_MAX));
+      if (aim_stick) {
+        lx += static_cast<int32_t>(std::lround(dx / len * out_len));
+        ly += static_cast<int32_t>(std::lround(dy / len * out_len));
+      } else {
+        rx += static_cast<int32_t>(std::lround(dx / len * out_len));
+        ry += static_cast<int32_t>(std::lround(dy / len * out_len));
+      }
+    }
   }
   // Drained unconditionally: deltas keep accumulating in OnMouseMove while the
   // mouse is off, and toggling it on would otherwise dump the whole backlog
@@ -476,15 +815,23 @@ void MnkInputDriver::RecenterCursorFromUIThread(int32_t x, int32_t y) {
 
 void MnkInputDriver::SetKeyState(uint16_t vk, bool down) {
   if (vk < 256) {
+    if (down && !key_down_[vk]) key_pressed_since_poll_[vk] = true;
     key_down_[vk] = down;
   }
 }
 
 void MnkInputDriver::OnKeyDown(rex::ui::KeyEvent& e) {
-  if (!IsEnabled() || !has_focus_)
+  if (!IsEnabled() || !has_focus_) {
+    static bool warned_gate = false;
+    if (!warned_gate) {
+      warned_gate = true;
+      REXLOG_INFO("MnK: key event dropped (enabled={}, has_focus={})", IsEnabled(), has_focus_.load());
+    }
     return;
+  }
   std::lock_guard lock(state_mutex_);
   uint16_t vk = static_cast<uint16_t>(e.virtual_key());
+  REXLOG_DEBUG("MnK: key down vk=0x{:02X}", vk);
   SetKeyState(vk, true);
 }
 
@@ -549,6 +896,15 @@ void MnkInputDriver::OnMouseMove(rex::ui::MouseEvent& e) {
       mouse_dx_ += float(x - prev_mouse_x_);
       mouse_dy_ += float(y - prev_mouse_y_);
     }
+    // DP1: mirror the deltas for the camera hook (drained by the app).
+    {
+      const float cdx = relative_mouse_mode_ ? float(e.dx()) : float(x - prev_mouse_x_);
+      const float cdy = relative_mouse_mode_ ? float(e.dy()) : float(y - prev_mouse_y_);
+      mouse_camera_dx.store(mouse_camera_dx.load(std::memory_order_relaxed) + cdx,
+                            std::memory_order_relaxed);
+      mouse_camera_dy.store(mouse_camera_dy.load(std::memory_order_relaxed) + cdy,
+                            std::memory_order_relaxed);
+    }
   }
   prev_mouse_x_ = x;
   prev_mouse_y_ = y;
@@ -558,6 +914,17 @@ void MnkInputDriver::OnMouseMove(rex::ui::MouseEvent& e) {
   }
 }
 
+// DP1 port 2026-09-05 (from dpour fork ce7d25d, mnk_input_driver.cpp).
+// Window::OnMouseWheel already forwards to every WindowInputListener, so
+// overriding it is all the plumbing the wheel needs. Same gating as
+// OnMouseDown; e.scroll_y() is in kScrollPerDetent units.
+void MnkInputDriver::OnMouseWheel(rex::ui::MouseEvent& e) {
+  if (!IsEnabled() || !has_focus_)
+    return;
+  std::lock_guard lock(state_mutex_);
+  wheel_accumulator_y_ += e.scroll_y();
+}
+
 void MnkInputDriver::OnLostFocus(rex::ui::UISetupEvent&) {
   has_focus_ = false;
   // Withdraw the request too, or an update queued before the focus loss grabs
@@ -568,6 +935,7 @@ void MnkInputDriver::OnLostFocus(rex::ui::UISetupEvent&) {
     std::memset(key_down_, 0, sizeof(key_down_));
     mouse_dx_ = 0.0f;
     mouse_dy_ = 0.0f;
+    wheel_accumulator_y_ = 0;  // DP1 port 2026-09-05
   }
   if (attached_window_) {
     ReleaseMouseCaptureFromUIThread(attached_window_);
diff --git a/src/input/sdl/sdl_input_driver.cpp b/src/input/sdl/sdl_input_driver.cpp
index c7c440d..19211ad 100644
--- a/src/input/sdl/sdl_input_driver.cpp
+++ b/src/input/sdl/sdl_input_driver.cpp
@@ -9,6 +9,7 @@
  * @modified    Tom Clay, 2026 - Adapted for ReXGlue runtime
  */
 
+#include <algorithm>
 #include <array>
 #include <filesystem>
 
@@ -23,8 +24,124 @@
 REXCVAR_DEFINE_STRING(hid_mappings_file, "gamecontrollerdb.txt", "Input",
                       "Path to SDL gamecontroller mappings file");
 
+// === DP1 port 2026-09-05 (from LittleBitUA/rexglue-sdk-dpour, dpour-v1.1.x) ==
+// DualSense (PS5) adaptive-trigger ambient effects. The Xbox 360 binary has no
+// notion of adaptive triggers; DualSense applies these in firmware, so the
+// cost on the recompiled game is zero. Tunable from TOML or the F4 overlay.
+REXCVAR_DEFINE_BOOL(dualsense_adaptive_triggers, true, "Input",
+                    "Master switch for DualSense (PS5) adaptive-trigger effects. "
+                    "When true, applies the per-trigger mode + parameters from "
+                    "the dualsense_rt_* and dualsense_lt_* cvars below on every "
+                    "controller-connect event. Works over USB and Bluetooth. "
+                    "Set false to leave both triggers in pass-through analog "
+                    "mode regardless of the per-trigger settings.");
+REXCVAR_DEFINE_STRING(dualsense_rt_mode, "weapon", "Input",
+                      "DualSense right-trigger ambient effect mode. One of: "
+                      "'off' (pass-through), 'feedback' (constant resistance), "
+                      "'weapon' (sharp click at a position - gun-trigger feel), "
+                      "'vibration' (buzz throughout the pull).");
+REXCVAR_DEFINE_INT32(dualsense_rt_start, 3, "Input",
+                     "Right-trigger effect start position (0=released, "
+                     "9=fully pulled). For 'feedback' this is where resistance "
+                     "begins; for 'weapon' this is the trigger-break start.");
+REXCVAR_DEFINE_INT32(dualsense_rt_end, 6, "Input",
+                     "Right-trigger effect end position (0-9). Only used by "
+                     "'weapon' mode - the click section runs from start to end. "
+                     "Ignored by 'feedback' and 'off'.");
+REXCVAR_DEFINE_INT32(dualsense_rt_strength, 5, "Input",
+                     "Right-trigger effect strength (0-8). Higher = harder to "
+                     "pull through the resistance / click point.");
+REXCVAR_DEFINE_STRING(dualsense_lt_mode, "feedback", "Input",
+                      "DualSense left-trigger ambient effect mode. Same value "
+                      "set as dualsense_rt_mode.");
+REXCVAR_DEFINE_INT32(dualsense_lt_start, 2, "Input",
+                     "Left-trigger effect start position (0-9).");
+REXCVAR_DEFINE_INT32(dualsense_lt_end, 0, "Input",
+                     "Left-trigger effect end position (0-9). Only used by "
+                     "'weapon' mode.");
+REXCVAR_DEFINE_INT32(dualsense_lt_strength, 4, "Input",
+                     "Left-trigger effect strength (0-8).");
+
 namespace rex::input::sdl {
 
+// Translate a user-friendly DualSense trigger mode string into the DS5
+// firmware mode byte. Unknown / invalid input returns 0x00 (off) so a typo
+// disables the effect rather than crashing.
+static uint8_t DualSenseTriggerModeByte(std::string_view name) {
+  if (name == "off" || name.empty()) return 0x00;
+  if (name == "feedback") return 0x01;
+  if (name == "weapon") return 0x02;
+  if (name == "vibration") return 0x06;
+  return 0x00;
+}
+
+static uint8_t DualSenseClampByte(int32_t v, int32_t lo, int32_t hi) {
+  return static_cast<uint8_t>(std::clamp(v, lo, hi));
+}
+
+namespace {
+constexpr size_t kDualSenseRtOffset = 10;
+constexpr size_t kDualSenseLtOffset = 21;
+
+// Fill an 11-byte trigger-effect slot from cvar values. mode_byte=0 leaves
+// the slot zero-initialized (i.e. effect off / pass-through). Packet layout
+// from SDL3 src/joystick/hidapi/SDL_hidapi_ps5.c (DS5EffectsState_t).
+void DualSenseFillTriggerEffect(uint8_t* slot, uint8_t mode_byte, int32_t start_cvar,
+                                int32_t end_cvar, int32_t strength_cvar) {
+  slot[0] = mode_byte;
+  switch (mode_byte) {
+    case 0x01:  // FEEDBACK: [start, strength]
+      slot[1] = DualSenseClampByte(start_cvar, 0, 9);
+      slot[2] = DualSenseClampByte(strength_cvar, 0, 8);
+      break;
+    case 0x02:  // WEAPON: [start, end, strength]
+      slot[1] = DualSenseClampByte(start_cvar, 0, 9);
+      slot[2] = DualSenseClampByte(std::max(end_cvar, start_cvar + 1), 1, 9);
+      slot[3] = DualSenseClampByte(strength_cvar, 0, 8);
+      break;
+    case 0x06:  // VIBRATION: [strength, frequency, start]
+      slot[1] = DualSenseClampByte(strength_cvar, 0, 8);
+      slot[2] = DualSenseClampByte(end_cvar, 0, 255);  // reuse end as frequency
+      slot[3] = DualSenseClampByte(start_cvar, 0, 9);
+      break;
+    default:  // OFF or unknown - leave zero
+      break;
+  }
+}
+
+void DualSenseApplyAdaptiveTriggers(SDL_Gamepad* controller, int ordinal) {
+  if (!REXCVAR_GET(dualsense_adaptive_triggers) ||
+      SDL_GetGamepadType(controller) != SDL_GAMEPAD_TYPE_PS5) {
+    return;
+  }
+  const uint8_t rt_mode = DualSenseTriggerModeByte(REXCVAR_GET(dualsense_rt_mode));
+  const uint8_t lt_mode = DualSenseTriggerModeByte(REXCVAR_GET(dualsense_lt_mode));
+  uint8_t effect[47] = {};
+  // ucEnableBits1: 0x04 = right trigger, 0x08 = left trigger. Only set the
+  // bit if that trigger has a non-off mode, so an "off" cvar value truly
+  // leaves the trigger in factory pass-through.
+  effect[0] = (rt_mode ? 0x04 : 0) | (lt_mode ? 0x08 : 0);
+  DualSenseFillTriggerEffect(effect + kDualSenseRtOffset, rt_mode,
+                             REXCVAR_GET(dualsense_rt_start), REXCVAR_GET(dualsense_rt_end),
+                             REXCVAR_GET(dualsense_rt_strength));
+  DualSenseFillTriggerEffect(effect + kDualSenseLtOffset, lt_mode,
+                             REXCVAR_GET(dualsense_lt_start), REXCVAR_GET(dualsense_lt_end),
+                             REXCVAR_GET(dualsense_lt_strength));
+  if (effect[0] == 0) {
+    REXLOG_INFO("SDL OnControllerDeviceAdded: DualSense at index {} - both triggers "
+                "configured 'off', leaving in pass-through.", ordinal);
+  } else if (!SDL_SendGamepadEffect(controller, effect, sizeof(effect))) {
+    REXLOG_WARN("SDL OnControllerDeviceAdded: DualSense adaptive-trigger setup failed: {}",
+                SDL_GetError());
+  } else {
+    REXLOG_INFO("SDL OnControllerDeviceAdded: DualSense at index {} - applied RT={} LT={} "
+                "from cvars.", ordinal, REXCVAR_GET(dualsense_rt_mode),
+                REXCVAR_GET(dualsense_lt_mode));
+  }
+}
+}  // namespace
+// === END DP1 port ============================================================
+
 namespace {
 
 // SDL clamps to SDL_MAX_RUMBLE_DURATION_MS, which is not a public constant.
@@ -467,6 +584,9 @@ void SDLInputDriver::OnControllerDeviceAddedLocked(const SDL_Event& event) {
   UpdateXCapabilities(state);
   controllers_.push_back(state);
 
+  // DP1 port: DualSense adaptive triggers (no-op for non-PS5 pads).
+  DualSenseApplyAdaptiveTriggers(controller, static_cast<int>(controllers_.size()) - 1);
+
   const int ordinal = static_cast<int>(controllers_.size()) - 1;
   SDL_SetGamepadPlayerIndex(controller, ordinal);
 
diff --git a/src/ui/CMakeLists.txt b/src/ui/CMakeLists.txt
index 5bd3dc5..291d5dd 100644
--- a/src/ui/CMakeLists.txt
+++ b/src/ui/CMakeLists.txt
@@ -29,6 +29,9 @@ set(REXUI_OVERLAY_SOURCES
     overlay/console_overlay.cpp
     overlay/console_commands.cpp
     overlay/settings_overlay.cpp
+    overlay/pso_compile_indicator_state.cpp
+    overlay/pso_compile_indicator_overlay.cpp
+    overlay/shader_compile_overlay.cpp
 )
 
 # Vulkan UI backend
diff --git a/src/ui/d3d12/d3d12_presenter.cpp b/src/ui/d3d12/d3d12_presenter.cpp
index 306118e..9da2f79 100644
--- a/src/ui/d3d12/d3d12_presenter.cpp
+++ b/src/ui/d3d12/d3d12_presenter.cpp
@@ -10,6 +10,7 @@
  */
 
 #include <algorithm>
+#include <atomic>
 #include <climits>
 #include <cmath>
 #include <memory>
@@ -187,6 +188,13 @@ bool D3D12Presenter::DispatchTemporalUpscaler(ID3D12GraphicsCommandList* command
 
   ffxContext* context = reinterpret_cast<ffxContext*>(&temporal_upscaler_context_);
   if (ffxDispatch(context, &dispatch_desc.header) != FFX_API_RETURN_OK) {
+    static std::atomic<bool> dispatch_failure_logged = false;
+    if (!dispatch_failure_logged.exchange(true)) {
+      REXLOG_WARN(
+          "D3D12Presenter: FidelityFX temporal upscaler dispatch failed ({}x{} -> {}x{}), "
+          "falling back to spatial FSR",
+          input_width, input_height, output_width, output_height);
+    }
     return false;
   }
 
@@ -867,6 +875,14 @@ Presenter::PaintResult D3D12Presenter::PaintAndPresentImpl(bool execute_ui_drawe
               command_list, effect_source_resource, effect_input_width, effect_input_height,
               effect_dest_resource, guest_output_flow.effect_output_sizes[i].first,
               guest_output_flow.effect_output_sizes[i].second, guest_output_paint_config);
+          // The FidelityFX dispatch binds its own descriptor heaps on the
+          // command list. The following effects (RCAS, bilinear) set root
+          // descriptor tables from the presenter's view heap, which is only
+          // valid while that heap is the one currently bound - otherwise it's
+          // D3D12 EXECUTION ERROR #708 SET_DESCRIPTOR_TABLE_INVALID. Hardware
+          // usually tolerates it, but with Steam's Game Recording hooked into
+          // the swap chain it produced a black screen (DPRecomp issue #13).
+          command_list->SetDescriptorHeaps(1, &view_heap);
           if (!temporal_dispatch_done) {
             D3D12_RESOURCE_BARRIER barrier_uav_to_rtv;
             barrier_uav_to_rtv.Type = D3D12_RESOURCE_BARRIER_TYPE_TRANSITION;
diff --git a/src/ui/keybinds.cpp b/src/ui/keybinds.cpp
index 9822f3b..6f7bf7c 100644
--- a/src/ui/keybinds.cpp
+++ b/src/ui/keybinds.cpp
@@ -141,6 +141,10 @@ static const std::unordered_map<std::string, VirtualKey> kKeyNames = {
     {"LMB", VirtualKey::kLButton},
     {"RMB", VirtualKey::kRButton},
     {"MMB", VirtualKey::kMButton},
+    // DP1 port 2026-09-05 (from dpour fork ce7d25d, src/ui/keybinds.cpp)
+    // Mouse wheel (synthetic 1-frame pulses, one per WHEEL_DELTA detent).
+    {"WheelUp", VirtualKey::kWheelUp},
+    {"WheelDown", VirtualKey::kWheelDown},
 };
 
 VirtualKey ParseVirtualKey(std::string_view name) {
diff --git a/src/ui/overlay/debug_overlay.cpp b/src/ui/overlay/debug_overlay.cpp
index 43d6c06..35646a5 100644
--- a/src/ui/overlay/debug_overlay.cpp
+++ b/src/ui/overlay/debug_overlay.cpp
@@ -9,6 +9,7 @@
  * @license     BSD 3-Clause License
  *              See LICENSE file in the project root for full license text.
  */
+#include <string>
 #include <rex/ui/overlay/debug_overlay.h>
 #include <rex/version.h>
 #include <imgui.h>
@@ -19,6 +20,12 @@
 
 namespace rex::ui {
 
+namespace {
+std::string g_build_stamp_override;
+}  // namespace
+
+void SetDebugOverlayBuildStamp(std::string_view stamp) { g_build_stamp_override = std::string(stamp); }
+
 DebugOverlayDialog::DebugOverlayDialog(ImGuiDrawer* imgui_drawer, FrameStatsProvider stats_provider)
     : ImGuiDialog(imgui_drawer), stats_provider_(std::move(stats_provider)) {}
 
@@ -88,7 +95,9 @@ void DebugOverlayDialog::OnDraw(ImGuiIO& io) {
   ImGui::End();
 
   // Build stamp watermark -- centered near bottom of screen
-  auto text_size = ImGui::CalcTextSize(REXGLUE_BUILD_STAMP);
+  const char* build_stamp =
+      g_build_stamp_override.empty() ? REXGLUE_BUILD_STAMP : g_build_stamp_override.c_str();
+  auto text_size = ImGui::CalcTextSize(build_stamp);
   float padding = ImGui::GetStyle().WindowPadding.x * 2.0f;
   float bottom_offset = io.DisplaySize.y * 0.03f;
   ImGui::SetNextWindowPos(ImVec2((io.DisplaySize.x - text_size.x - padding) * 0.5f,
@@ -99,7 +108,7 @@ void DebugOverlayDialog::OnDraw(ImGuiIO& io) {
                    ImGuiWindowFlags_NoDecoration | ImGuiWindowFlags_NoBackground |
                        ImGuiWindowFlags_NoInputs | ImGuiWindowFlags_NoNav |
                        ImGuiWindowFlags_NoSavedSettings | ImGuiWindowFlags_AlwaysAutoResize)) {
-    ImGui::TextUnformatted(REXGLUE_BUILD_STAMP);
+    ImGui::TextUnformatted(build_stamp);
   }
   ImGui::End();
   ImGui::PopStyleColor();
diff --git a/src/ui/rex_app.cpp b/src/ui/rex_app.cpp
index b94fe9e..5fb41ee 100644
--- a/src/ui/rex_app.cpp
+++ b/src/ui/rex_app.cpp
@@ -25,6 +25,8 @@
 #include <rex/ui/overlay/console_overlay.h>
 #include <rex/ui/overlay/debug_overlay.h>
 #include <rex/ui/overlay/settings_overlay.h>
+#include <rex/ui/overlay/pso_compile_indicator_state.h>
+#include <rex/ui/overlay/shader_compile_overlay.h>
 #include <rex/audio/audio_system.h>
 #include <rex/audio/sdl/sdl_audio_system.h>
 #include <rex/input/input_system.h>
@@ -394,6 +396,11 @@ void ReXApp::SetupOverlays(rex::ui::Presenter* presenter, rex::ui::ImmediateDraw
   // gated eager font upload in SetImmediateDrawer is skipped (font uploads
   // lazily on the first Draw instead).
   imgui_drawer_->SetPresenterAndImmediateDrawer(presenter, drawer);
+  // DP1 v1.1: PSO compile indicator (self-gates on show_shader_compile_indicator).
+  if (!pso_compile_indicator_overlay_) {
+    pso_compile_indicator_overlay_ =
+        std::make_unique<ui::PsoCompileIndicatorDialog>(imgui_drawer_.get());
+  }
   rex::ui::RegisterBind("bind_debug_overlay", "F3", "Toggle debug overlay", [this] {
     if (debug_overlay_) {
       debug_overlay_.reset();
@@ -457,25 +464,60 @@ void ReXApp::LaunchModule() {
     }
 
     auto* graphics_system = runtime_->graphics_system();
-    if (graphics_system && !runtime_->cache_root().empty()) {
-      uint32_t title_id = runtime_->kernel_state()->title_id();
-      if (title_id != 0) {
-        REXLOG_INFO("Initializing shader storage for title {:08X}...", title_id);
-        graphics_system->InitializeShaderStorage(runtime_->cache_root(), title_id, true);
-      }
+    const uint32_t title_id =
+        runtime_->kernel_state() ? runtime_->kernel_state()->title_id() : 0;
+    const bool has_shader_storage =
+        graphics_system && !runtime_->cache_root().empty() && title_id != 0;
+    if (!has_shader_storage) {
+      FinishModuleLaunch(std::move(main_thread));
+      return;
+    }
+    REXLOG_INFO("Initializing shader storage for title {:08X}...", title_id);
+
+    // DP1 v1.1 (Downpour port): run the blocking storage replay on a worker
+    // thread and keep the UI thread pumping so a "Pipelines N / M" toast is
+    // visible instead of a frozen window; the toast resumes the guest main
+    // thread once the replay has finished.
+    if (imgui_drawer_) {
+      auto& progress = rex::graphics::d3d12::PsoCompileIndicatorStateRef();
+      progress.storage_finished.store(false, std::memory_order_release);
+      progress.storage_shaders_translated.store(0, std::memory_order_relaxed);
+      progress.storage_pipelines_total.store(0, std::memory_order_relaxed);
+      progress.storage_pipelines_created.store(0, std::memory_order_relaxed);
+      auto cache_root = runtime_->cache_root();
+      auto main_thread_shared =
+          std::make_shared<system::object_ref<system::XThread>>(std::move(main_thread));
+      // Owned by the drawer's dialog list; deleted after Close().
+      new rex::ui::ShaderCompileDialog(
+          imgui_drawer_.get(), "Preparing shaders",
+          "Warming the pipeline cache; only the first launch after an update takes a while.",
+          [this, main_thread_shared]() mutable {
+            FinishModuleLaunch(std::move(*main_thread_shared));
+          });
+      std::thread([graphics_system, cache_root = std::move(cache_root), title_id]() {
+        graphics_system->InitializeShaderStorage(cache_root, title_id, true);
+        rex::graphics::d3d12::PsoCompileIndicatorStateRef().storage_finished.store(
+            true, std::memory_order_release);
+      }).detach();
+      return;
     }
 
-    OnPostLaunchModule(main_thread.get());
-    main_thread->Resume();
+    graphics_system->InitializeShaderStorage(runtime_->cache_root(), title_id, true);
+    FinishModuleLaunch(std::move(main_thread));
+  });
+}
 
-    module_thread_ = std::thread([this, main_thread = std::move(main_thread)]() mutable {
-      main_thread->Wait(0, 0, 0, nullptr);
-      OnGuestThreadExit(main_thread.get());
-      REXLOG_INFO("Execution complete");
-      if (!shutting_down_.load(std::memory_order_acquire)) {
-        app_context().CallInUIThread([this]() { app_context().QuitFromUIThread(); });
-      }
-    });
+void ReXApp::FinishModuleLaunch(system::object_ref<system::XThread> main_thread) {
+  OnPostLaunchModule(main_thread.get());
+  main_thread->Resume();
+
+  module_thread_ = std::thread([this, main_thread = std::move(main_thread)]() mutable {
+    main_thread->Wait(0, 0, 0, nullptr);
+    OnGuestThreadExit(main_thread.get());
+    REXLOG_INFO("Execution complete");
+    if (!shutting_down_.load(std::memory_order_acquire)) {
+      app_context().CallInUIThread([this]() { app_context().QuitFromUIThread(); });
+    }
   });
 }
 
diff --git a/src/ui/window_sdl.cpp b/src/ui/window_sdl.cpp
index 2251429..cee7285 100644
--- a/src/ui/window_sdl.cpp
+++ b/src/ui/window_sdl.cpp
@@ -474,9 +474,24 @@ void WindowSDL::HandleWindowEvent(SDL_Event& event) {
       break;
     }
     case SDL_EVENT_WINDOW_FOCUS_GAINED:
+      // DP1 (2026-09-06): after Alt+Tab back into a borderless fullscreen
+      // window, the Windows shell sometimes keeps the taskbar above it (the
+      // "rude app" detection is not re-run on plain activation). Re-assert
+      // fullscreen so SDL issues a fresh SetWindowPos over the monitor rect.
+      // Re-entering fullscreen is a no-op inside SDL when the window is already
+      // fullscreen, so hold the window above the taskbar with always-on-top
+      // while it is focused and drop the flag when focus leaves (other apps
+      // must be able to cover the game after Alt+Tab).
+      if (IsFullscreen() && (SDL_GetWindowFlags(sdl_window_) & SDL_WINDOW_FULLSCREEN)) {
+        SDL_SetWindowAlwaysOnTop(sdl_window_, true);
+        SDL_RaiseWindow(sdl_window_);
+      }
       OnFocusUpdate(true, destruction_receiver);
       break;
     case SDL_EVENT_WINDOW_FOCUS_LOST:
+      if (SDL_GetWindowFlags(sdl_window_) & SDL_WINDOW_ALWAYS_ON_TOP) {
+        SDL_SetWindowAlwaysOnTop(sdl_window_, false);
+      }
       OnFocusUpdate(false, destruction_receiver);
       break;
     case SDL_EVENT_WINDOW_EXPOSED:
